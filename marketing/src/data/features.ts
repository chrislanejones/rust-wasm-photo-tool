// GENERATED — do not edit. Run: node marketing/scripts/gen-trail-data.mjs
//
// The feature list, from docs/Features.md — the repo's own canonical list.
// 55 features across 2 groups.

export interface Feature {
  name: string;
  body: string;
}

export interface FeatureGroup {
  name: string;
  items: Feature[];
}

export const FEATURES: FeatureGroup[] = [
  {
    name: "Image Processing (Rust/WASM)",
    items: [
      { name: "Clone Stamp", body: "Alt+Click source, paint to clone with adjustable size, hardness, opacity, spacing" },
      { name: "Red Stamps", body: "REJECTED / APPROVED / DRAFT / CONFIDENTIAL / UNDER REVIEW presets; JS renders label to OffscreenCanvas, Rust scales to brush size via bilinear resize and composites with \"Red Stamp\" history entry" },
      { name: "Edit group (Crop · Transform · Perspective · Color Picker · Layers · Canvas Size · Guides · Rulers and Grid)", body: "each is its own sub-tool with its own panel section and its own URL, split out of the single combined tool they used to share. Crop: interactive SVG overlay with rule-of-thirds guides and 8 draggable resize handles, committed through Rust. Transform: flip horizontal/vertical, rotate 90° CW/CCW. Layers: pick a layer, then move, scale or mask it. Canvas Size: grow or shrink the background canvas without resampling. Guides: draggable horizontal and vertical alignment lines" },
      { name: "Select group", body: "six ways to pick pixels, one exclusive set (Select on the rail): Magic Wand (flood-fill a region within a color tolerance), Edge-aware (the same fill, stopped at the outline by an edge map), Magnetic Lasso (click anchors and the wire path-finds along the edges), Color Range (every pixel near the clicked color, anywhere in the image), and Rectangle and Ellipse marquees. The flood-fill, mask and delete are all Rust (magic_wand_select / delete_selection), with Alt+A select-all and Alt+D deselect" },
      { name: "Placement grid", body: "a nine-cell grid in the Text and Shapes panels drops the selected text or shape into that ninth of the canvas. Rust centers the bounding box in the cell (align_annotation) as a single undo step" },
      { name: "Text Shadow", body: "soft drop shadow on the text glyphs and/or the background box, in the Text panel below the color swatch: shared color / opacity / offset / blur, rendered in Rust (offset + box-blurred silhouette, tile grown to fit so Align sees it) and persisted with the edit" },
      { name: "Security tab", body: "EXIF keep/strip moved out of Compress into Settings → Security as a saved preference (applies to every export path); GPS / capture-time / lens stays or goes by your choice" },
      { name: "Responsive / snapped windows", body: "one shared useBreakpoint hook: below ~1000px the top bar goes icon-only, below ~900px the side panels float as overlay drawers (scrim, one at a time) instead of crushing the canvas, below ~600px the editor steps aside for a phone surface — upload, add and view the gallery, no editing — with a gear that opens Theme, Motion and Sync. Reduce Motion suppresses the layout slides" },
      { name: "Keyboard accessibility", body: "Skip-to-canvas link, landmark roles + labels (toolbar / panels / canvas), accessible names on the tool buttons, and Escape-to-close + role=\"dialog\" semantics on the modals; on top of the existing tool / number / Alt shortcuts" },
      { name: "Resize & Compress", body: "one tile, one panel. Both halves move the same two numbers (Web Performance Gain and PageSpeed Insights Score read dimensions AND format/quality together), so they share a panel the way Squoosh does: the scores on top, then Resize, then Compress. Resampling is fully in WASM with no canvas round-trip. One Apply button, named for what is pending — \"Apply Resize\" when only the dimensions moved, \"Apply Compression\" when only quality/format/method moved, \"Apply Compression & Resize\" when both did. Dimensions-only re-saves in the photo's own format at full quality, so the quality slider is left alone" },
      { name: "Adjustments", body: "Brightness, Contrast, Saturation, Shadows, Highlights, Blur and Sharpen as sliders (Enhance → Adjustments); each adjustment is a separate undo snapshot" },
      { name: "Levels", body: "black point, white point and midtones (Enhance → Levels), set against a histogram of the photo with all three marked on it. The photo updates as you drag: the preview works from a copy of the layer, recomputed through a 256-entry lookup table on every move, so moves never compound. Apply makes it one undo step; leave the panel without applying and the photo goes back (src/levels.rs)" },
      { name: "Presets", body: "twelve one-click looks (Enhance → Presets): Enhance, Vivid, Fade, Dark, Warm, Cool, Mono, Noir, Airy, Moody, Recover and Lift. Hover one and it previews on your own photo; click it and that is a single undo step. A preset is not a new filter — it is five numbers (brightness, contrast, saturation, shadows, highlights) run through filters the engine already had (apply_stack in src/presets.rs). The engine has no hue or temperature control, so Warm and Cool are saturation looks, not color temperature" },
      { name: "Histogram", body: "live RGB / Luma scope in the Review panel, computed in Rust (calculate_histogram) straight from the composite buffer — no per-frame offscreen-canvas sampling" },
      { name: "Fast integer compositing", body: "all alpha blending (blend_pixel, blend_over) uses integer source-over math instead of per-pixel float ÷255.0, verified identical to the old result within ±1" },
      { name: "Color Picker", body: "its own sub-tool under Edit → Color Picker; hovering the canvas shows a floating 11×11 magnifier (sourced from Rust get_pixel_region); clicking picks the pixel color and sets it as both brush and text color" },
      { name: "Blur Brush", body: "Box-blur with stroke-based region masking; configurable radius and intensity; its own sub-tool under Create → Blur Brush" },
      { name: "Arrows", body: "Anti-aliased arrows with arrowhead (single or double), drawn directly on the pixel buffer; Create → Arrow" },
      { name: "Shapes", body: "Rectangles, circles, hand-drawn circles, and lines rendered in WASM; a Sloppiness slider ramps the outline from firm to hand-drawn; Create → Shapes, with Arrow and Pins as their own sub-tools beside it" },
      { name: "Paint / Brush", body: "Freehand painting via WASM paint_dab + paint_stroke_to; configurable brush size, color, and opacity; Create → Brush, with Pen and Blur Brush as sibling sub-tools" },
      { name: "Pen (Vector Paths)", body: "Photoshop-style Bézier pen (Create → Pen): click to drop corner anchors, click-drag to pull smooth curve handles, and grab any anchor or handle to reshape the path. Enter closes it, Esc finishes it open. An optional solid background fills the closed interior (Rust scanline polygon fill, under the stroke). Committed paths stay re-editable — click one to re-open its anchors/handles — and round-trip through history, photo-switch, reload, and cloud sync as a kind-7 ShapeAnnotation (cubic control sequence flattened with de Casteljau)" },
      { name: "Perspective / Distort / Skew", body: "one four-corner quad with three drag rules (Edit → Perspective), and it works on the vector objects the app draws: click a square, a circle, a line, an arrow, a pin, a pen path or a piece of text and the corners are stored ON that object as fractions of its own box, applied at render time. So the warped thing is still an object — recolor it, move it, resize it, undo it, or re-select it and adjust the same quad, and the perspective comes along. With nothing picked the quad warps the pixels under it instead, which is destructive and says so. Apply / Reset / Cancel sit on the canvas under the box as well as in the panel; Esc cancels and takes the box off the canvas. Only the objects on the active layer are pickable, and switching layers drops the pick. The homography solve and the resampler are Rust (src/perspective.rs); the drag rules are TypeScript on purpose, because they run per pointermove and a worker round trip there is the banked-lag failure the brush arc spent five releases removing" },
      { name: "Text", body: "Click-to-place text with a choice of typeface, size, weight and color. The font menu picks real fonts: Liberation Sans, Serif and Mono (SIL OFL 1.1), regular and bold, fetched as static files and handed to the engine at runtime (register_font), so committed text is drawn in the face you previewed. Up to 8 recent texts re-open the canvas text box at the last used position, restoring all text settings" },
      { name: "Emoji Stamp", body: "Browser renders emoji to OffscreenCanvas, pixels sent to WASM stamp_pixels() for alpha compositing; Create → Emoji" },
      { name: "Export", body: "Lossless PNG via Rust encoder, JPEG/WebP/AVIF via browser" },
      { name: "Blank Canvas", body: "start a new document from the upload dialog: dimensions + a page-size preset (FHD / Square / Story / 4×6 / 5×7 / 8×10) and a White / Black / hex / Transparent background; the fill is generated in Rust (blank_png → codec::export_png) with no canvas round-trip" },
      { name: "Thumbnails", body: "256px WebP thumbnails built from the already-decoded working pixels via Rust resize_pixels (uploads decode once, not twice); working canvas stays at ≤2048px" },
      { name: "Copy/Paste Regions", body: "Cross-photo pixel compositing with alpha blending; paste from clipboard supported" },
      { name: "History", body: "Undo/redo with labeled snapshots (including dimensions for crop/resize/rotate correctness), jump-to, delete entry. Depth is 50 steps by default and up to 1000 in Settings → General. The status bar shows Undo NN% — how deep undo can reach right now as a share of that setting" },
      { name: "Per-photo Edit Persistence", body: "Switching photos saves the full WASM canvas + undo/redo stack to IndexedDB (PNG-encoded per snapshot). Switching back restores the exact edit session — same canvas state, same undo history, all redo steps intact" },
      { name: "Layers", body: "a real layer stack per photo: 8 layers per image, 16 on Pro. Add, rename, hide, reorder, set opacity, delete and flatten from Review → Layers; Edit → Layers moves, resizes and masks the picked layer (paint the mask, invert it, apply it or remove it) and adds a color overlay. The stack lives in the Rust engine and is saved with the edit, so it survives a reload" },
      { name: "OpenRaster export and import", body: "Settings → Import / Export writes the full project, every layer, to an .ora file that opens in Krita, GIMP and other editors, and opens an .ora as a new photo with its layer stack restored" },
    ],
  },
  {
    name: "UI (React)",
    items: [
      { name: "Animated Panels", body: "Staggered entrance: TopBar → Sidebar → Gallery (Framer Motion springs)" },
      { name: "Pan (H or Space)", body: "H is the hand key: hold it and the pan ends on release, tap it and the pan stays on until the next tap or Esc. Holding Space still pans. All tool handlers are bypassed during a pan, and typing h in a text field does nothing" },
      { name: "A/B Compare Slider", body: "Squoosh-style draggable divider; overlay is positioned exactly over the canvas bounding box (tracks zoom/pan via ResizeObserver) so before/after layers are always pixel-aligned" },
      { name: "Multi-photo Gallery", body: "Bottom strip with thumbnails, add/remove/switch/duplicate (content-addressed, zero-copy; carries edits); PgUp/PgDn cycling; multi-select with Export / Delete / Duplicate / Unselect; header count + per-tier limit (i) tooltip; originals preserved in IndexedDB at full resolution regardless of working-copy downscale" },
      { name: "Review Panel", body: "Right-side panel (Alt+R) with a header toggle group that opens up to three stacked sections sharing the body height (1 full / 2 halves / 3 thirds, each scrollable):" },
      { name: "Upload", body: "Drag-and-drop modal with file browser and paste-from-clipboard (Ctrl+V / paste button)" },
      { name: "Export Dropdown", body: "PNG, JPEG, WebP, AVIF format selector in the top bar" },
      { name: "Keyboard Shortcut Modal", body: "Alt+/ opens a full reference overlay grouped by category (two columns per group)" },
      { name: "Diagnostics Log", body: "Alt+Delete, or Toggle Diagnostics in the command palette, opens a log overlay: engine, IndexedDB, Convex, AI and console entries for the current tab, up to 500 of them, held in memory only — never persisted and never sent anywhere" },
      { name: "Erasers and AI removal", body: "three sub-tools over one tool id (ai, unchanged for shortcut/persistence/routing). Create → Eraser: drag on the canvas to scrub the active layer to transparent — local, free, no sign-in. Create → Magic Eraser: paint over something and PatchMatch fills it in, on your machine, free. Enhance → AI (Pro, needs the network): Background Removal (rembg) and Object Removal (LaMa), both run on Replicate. Object Removal is a brush on the real canvas — you paint the mask over the object at whatever zoom you are on, with Undo Stroke and Clear Mask, and the mask stays over the object while the job runs" },
      { name: "Light / Dark / System theme", body: "full light & dark palettes (warm earth-tone dark, warm-paper light) driven by CSS-variable tokens in styles.css (:root light, .dark dark, @custom-variant dark); \"System\" follows the OS via matchMedia and updates live. Pre-paint guard in index.html (no FOUC). Set in Settings → Appearance, persisted to localStorage + Convex. JetBrains Mono + DM Sans throughout" },
      { name: "Rulers & Grids", body: "toggleable top/left pixel rulers (tick labels track zoom) + a configurable non-destructive grid overlay: square px spacing, golden-ratio lines, or N×M divisions, with color + opacity. Set in Settings → Rulers & Grids, persisted. The grid-layout geometry is computed in Rust (grid_lines WASM export) as the single source, projected to an SVG overlay on the canvas" },
      { name: "State management (Zustand)", body: "the editor's state lives in seven Zustand stores (UI, tool, gallery, annotation, guides, perspective, text box) instead of AppShell local state; components subscribe with atomic selectors for minimal re-renders, and durable prefs (last master-bar tab, tool sub-modes) persist to IndexedDB via a write-deduped StateStorage adapter. Behavior-preserving groundwork for splitting AppShell into feature modules. See State Management" },
      { name: "Five-group toolbar", body: "the rail is five groups (Enhance · Select · Create · Edit · Batch) owning 38 sub-tools, replacing eleven flat tools. One registry (features/tools/toolGroups.ts) defines them, and the rail, the sub-tool row, the digits 1–5, the shortcut sheet, the canvas dispatch and the cursor all derive from it — there is no second table to fall out of step. Legacy tool ids are untouched underneath, so persistence and old links keep working" },
      { name: "Sub-tool routing", body: "every sub-tool has its own address: #/create/brush, #/edit/color-picker, #/select/ellipse. Thirty-five older URL shapes redirect to the sub-tool they meant, each pinned by a test. The three Edit sub-tools that share a tool underneath used to collapse onto one URL; they no longer do" },
      { name: "Sub-tool canvas dispatch", body: "the lit sub-tool decides what a drag does and which cursor you get, instead of the whole parent tool sharing one. A sub-tool with no canvas gesture idles by default rather than inheriting whatever the clone stamp was doing" },
      { name: "Color picker history", body: "eyedroppered colors land in a Recent Colors list under Edit → Color Picker: click to re-apply to brush and text, ✕ to forget, Clear to empty. Same list UI as the guides list. Per-session, not persisted" },
      { name: "OCR", body: "reading text out of an image is its own sub-tool (Create → OCR) rather than a mode buried inside the Text panel" },
      { name: "Directional duplicate pad", body: "a rectangle or circle picked out of Review → Reselect gets a d-pad on its row; pressing it puts four ⊕ around the shape on the canvas. Each press lays another same-sized copy, clear of the original, in that direction, and each side counts independently — ← twice gives two marching left, and a later ↑ goes above the original rather than above the last copy. Built for laying out a diagram from repeated boxes. Rectangles and circles only; every other shape kind keeps a disabled placeholder so the row shape stays constant down the list. Offsets are computed from the shape's own bounding box (lib/duplicatePadGeometry.ts) and the copy itself is an engine-side struct clone (duplicate_shape_annotation)" },
      { name: "Duplicate from the Reselect row", body: "any placed text or shape copies from its own row, offset down-right. The engine clones the annotation struct rather than re-adding it from a property list, so a shadow, rotation, background, corner radius or perspective warp travels with the copy; a test walks every field the engine emits rather than a hand-listed set, so a field added later is covered the moment it exists" },
      { name: "Stroke Stabilizer, everywhere", body: "the pulled-string \"lazy mouse\" leash reaches the Paint brush, the Eraser, the blur brush, pixelate, redact and the clone stamp from a single setting. One leash implementation (src/stabilizer.rs) with its level table exported to JS as stabilizer_leash, so no consumer re-derives the numbers. On the clone stamp the source offset is preserved exactly — a lagging tip samples a source lagging by the same vector — and a move inside the leash costs zero recomposites, because every stroke engine returns whether it actually stamped" },
      { name: "Settings sync — every tab, every signed-in device", body: "change a setting in one tab and the others are already right when you switch to them, with no reload and no account: a BroadcastChannel carries it across tabs on this device, which is sync a signed-out user gets too. Sign in and the same change reaches your phone and your other computers over one reactive Convex query. Three documents travel — your preferences, the panels/tabs you left open with the command palette's recent list, and which mode each tool was in — replicated whole, with the deciding rule (adopt / push / hold / idle) as one pure function that has its own spec, and a server that refuses a write based on a revision someone else has already moved past. Your photos are not in it: originals, edits and the gallery stay in the browser that made them, exactly as they do signed out. Settings → General shows what sync is doing and can delete the synced copy from your account. See ADR-061" },
      { name: "Share links, and Settings → Shared", body: "Export → Share link uploads a flattened PNG and copies a public link that works for anyone, signed in or not. Settings → Shared lists every link you have made: its image, how many times it was opened, when it was last opened, and a bar for each of the last 30 days, with your totals on top. Each link can stop after a number of views, stop on a date, or both; hitting a limit pauses it and keeps the image, so raising the limit turns it back on. Pause, resume and delete by hand too. A stopped link tells the visitor why and sends no image. Each opening is stored as a time only — no address, no browser, no account" },
    ],
  },
];
