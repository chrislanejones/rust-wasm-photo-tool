/target
/pkg

# Convex auto-generated AI context files
convex/_generated/ai/
node_modules
app/node_modules
app/dist

# Environment files — never commit secrets
.env.local
.env.*.local
app/.env.local
app/.env.*.local

# Added by roll_repo_for_ai.sh
roll_repo_for_ai.sh

# Added by roll_repo_for_ai.sh
rolled_repo/

# Local Netlify folder
.netlify

# Claude Code / agent tooling
.claude/
.agents/
.hallmark/
AGENTS.md
CLAUDE.md
skills-lock.json

# Build output (Netlify builds from source)
www-dist

# Windows/WSL alternate-data-stream artifacts
*:Zone.Identifier

# Local planning / action-plan docs (not for the repo)
action-plan.md
docs/.hidden/

# Scalar wasm build for the SIMD benchmark (rebuild on demand — see bench.html)
pkg-scalar/

# Overnight run docs
RUN_SCHEDULE.md
NIGHT*_PROMPT.md
DAY*_PROMPT.md
E2E_PROMPT.md
SESSION_LOG.md
PARKING_LOT.md

# Overnight-run tooling (any *_PROMPT.md + the status dashboard)
*_PROMPT.md
runs-status.sh

# Playwright e2e artifacts
/test-results/
/playwright-report/
/playwright/.cache/
e2e.log

install.log

# tooling debris — MCP browser logs, stray dist copies
.playwright-mcp/
/www-dist-check/
