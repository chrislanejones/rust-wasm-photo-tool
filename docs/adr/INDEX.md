# Architecture Decision Records

Why the load-bearing, hard-to-reverse decisions were made the way they
were. New ADRs land as **Draft** (a proposal for review) and are flipped
to **Accepted** / **Rejected** by a human. Procedure + template:
`~/.claude/skills/adr/SKILL.md`.

Numbers are sequential and permanent — a superseded ADR keeps its number
and gains a `Superseded by ADR-NNN` status; it is never renumbered or
deleted.

> **Status audit (2026-07-09, against v7.8 / `d9960f6`):** every ADR
> below was checked against actual git history and code (grep, not
> assumption) — see each file's added status line for the evidence,
> and `SESSION_LOG.md` on the `docs/adr-status-audit` branch for the
> full trail. This audit **supersedes** the 2026-07-07 review note
> that used to live here. Four flagged verified-shipped were confirmed
> Accepted by Chris the same day. Note also: 003/004 (op-log / tile
> buffer) merged to master later on 2026-07-09 as feature-gated,
> off-by-default code (fast-forward, `1703670`) — that's a merge, not
> a wiring, so they correctly stay Draft/blocked below.

| ADR | Title | Status | Date |
| --- | ----- | ------ | ---- |
| [000](000-template.md) | Template (not a decision) | — | — |
| [001](001-originals-lazy-migration-to-dexie.md) | Originals store: lazy read-through migration to Dexie | Accepted (2026-07-09) — shipped v7.5 (`6ccd67b`) | 2026-07-02 |
| [002](002-tool-module-registry.md) | Canvas tools become registry modules instead of hand-wired hooks | Draft — blocked (Stage 4 not started; AppShell still hand-wires 8 tool hooks) | 2026-07-02 (backfilled) |
| [003](003-operation-log-undo.md) | Operation log replaces snapshot undo | **Accepted (2026-07-17)** — four-check A/B passed; defaults ON in v7.36, `ih_oplog_undo` now a kill switch | 2026-07-02 (backfilled) |
| [004](004-tile-buffer.md) | 256×256 tiles become the canonical image representation | **Accepted (2026-07-17)** — tiles compiled + flush path default ON in v7.36 (`ih_tiles_flush` now a kill switch); single-layer scope stands | 2026-07-02 (backfilled) |
| [005](005-codec-worker-fallback.md) | Encoding and thumbnailing move to a worker, with mandatory main-thread fallback | Accepted (2026-07-09) — shipped v7.7 (`906082b`) | 2026-07-02 (backfilled) |
| [006](006-render-cache-disposable.md) | Working copies become a disposable render cache; truth is original + op log | **Accepted (2026-07-17)** — real-gallery A/B passed (stroke + AI round trips restored from the log, Canvas intact); persistence default ON in v7.36, working copy stays the fallback | 2026-07-02 (backfilled) |
| [007](007-overnight-worktree-runs.md) | Unattended agent runs happen on disposable worktrees in auto mode | Accepted (2026-07-09) — matches standing practice | 2026-07-02 (backfilled) |
| [008](008-svg-rasterized-at-import.md) | SVG imports are rasterized to PNG at the import boundary | Accepted (2026-07-09) — shipped v7.8 (`d9960f6`) | 2026-07-07 |
| [009](009-coop-coep-clerk-spike.md) | COOP/COEP vs. Clerk sign-in spike | **Accepted (2026-07-13)** — confirmed live in a real browser (`credentialless`; `crossOriginIsolated === true` logged out AND logged in, through a real sign-in). Residual: OAuth-popup path + Convex WS untested | 2026-07-09 |
| [010](010-metadata-scrub-privacy-modes.md) | Export metadata scrub gets a GPS-only mode alongside the existing full strip | Draft | 2026-07-10 |
| [011](011-parallel-kernels-rayon-threads.md) | Parallel kernels via wasm-bindgen-rayon, gated on COOP/COEP | Draft — **gate now OPEN** (ADR-009 confirmed live 2026-07-13); blocker gone, build not yet shipped | 2026-07-10 |
| [012](012-oplog-document-model.md) | The op log replays over a document (pixels + live annotation lists), not the flattened composite | **Accepted (2026-07-17)** — engine parity green incl. the app's exact import flow | 2026-07-11 |
| [013](013-oplog-undo-hash-fallback.md) | Op-log undo activates behind a composite-hash sync check, with snapshot fallback | **Accepted (2026-07-17)** — verified live on the shipped defaults; fallback permanent | 2026-07-11 |
| [014](014-magnetic-lasso-smart-brush-cost-map.md) | Magnetic lasso and Smart Brush both stand on one shared edge cost map | Draft — kernels tested + wired behind `ih_smart_edge` (default OFF); needs a human canvas check of the FEEL | 2026-07-13 |
| [015](015-hash-routing-palette-nav.md) | Hash routing + URL params; the palette navigates via the route registry | Draft | 2026-07-13 |
| [016](016-canvas-is-document-metadata.md) | The Canvas is document metadata, not a logged layer | **Accepted (2026-07-17)** — the real-gallery DEFAULT-document check passed: Canvas rebuilt from metadata on restore, composite byte-exact (the 07-14 data loss was fixed in v7.33; the 07-17 "canvas gone" report was a checkerboard-backdrop rendering bug, not data loss) | 2026-07-13 |
| [017](017-tiles-compiled-into-shipped-wasm.md) | The tiles/op-log feature is compiled into the shipped WASM | **Accepted (2026-07-17)** — Phase-1 dogfood = the four-check A/B; flags default ON in v7.36 with "0" kill switches. Size baseline 731,595 B (+71,780) | 2026-07-13 |
| [018](018-patchmatch-object-removal-scalar-day-1.md) | PatchMatch object removal ships as a scalar, single-resolution kernel behind a flag | **Accepted (2026-07-18)** — Chris confirmed the fill on a real canvas; merged to master, `ih_patchmatch` stays OFF by default (dogfood before defaulting, same as op-log). **Day-2 rayon REJECTED 2026-07-19** — byte-identical but 8–31× slower; kept unmerged on `perf/patchmatch-rayon` as a recorded negative result, see the ADR's follow-up section | 2026-07-18 |
| [019](019-opt-in-precache-service-worker.md) | The app shell ships behind an opt-in, precache-only service worker | **Accepted (2026-07-19)** — merged dark; ship-dark re-verified on the production build at merge (no `sw.js`/workbox/`version.json`, zero `serviceWorker` refs in the bundle). ⚠️ Once an ON build ever ships, turning it off means deploying `VITE_ENABLE_SW=kill` — **never** unsetting the flag | 2026-07-19 |
| [020](020-react-compiler-rules-adopted-per-rule.md) | The React Compiler rule set is adopted rule by rule, and the imperative ref layer stays | **Accepted (2026-07-22)** — 123 problems / 66 errors at `5621471`; 6 fixed (`e88a0d8`), **32 declined on purpose**, 28 flagged. `static-components` + `purity` on as `error`, `refs` held pending a QC'd `useLatestRef`/`useEffectEvent` migration, `set-state-in-effect` never gated. ⚠️ Read the declines before "fixing" them — `useBrushPreview`, the boot Clerk race and the IndexedDB dirty flag all change behavior if rewritten. Landmine: `SubscriptionButton.tsx:172` | 2026-07-22 |
| [021](021-select-is-a-first-class-tool.md) | Select is a first-class tool, not a sub-mode of Adjust | **Draft** — tool #11, id `select` (first id born matching its label); engine `rect_select`/`ellipse_select` marquee; `adjustMode`/`selectionMode`/Click-to-select toggle deleted; shortcut `S` = first letter key in `TOOL_BY_KEY`; legacy `#/tool/adjust/select` redirects. Awaiting Chris's hands-on smoke + accept | 2026-07-23 |
| [022](022-selection-modes-are-one-exclusive-set.md) | The Select tool's six modes are one exclusive set | **Draft** — supersedes ADR-021 in part: `SelectionKind` absorbs rect/ellipse, `selectionShape` deleted, mode picks the GESTURE (click-once / lasso session / drag-only). ADR-021 rejected marquee-as-a-tile to avoid mode-switching; reversed because the two-axis panel was unreadable — "I don't get the rect and circle". Rect→Rectangle, Magnetic→Magnetic Lasso (`Magnet` icon), 3-up grid, per-mode routes. Select is now **keyless** (`S` removed; a digit comes with the next UI pass) | 2026-07-25 |
| [023](023-toolbar-is-five-groups.md) | The toolbar is five groups of sub-tools, not a flat tool list | **Draft** — written after the fact; records v7.51–v7.53 as shipped. Digits `1`–`5` select the GROUP (there is no second digit axis); `6`–`0` and `-` freed. `features/tools/toolGroups.ts` is the single definition site and everything derives from it — the parts that didn't (the status bar) drifted for six releases and told users to press `2` on Edit, which is Select. Legacy tool ids deliberately NOT renamed | 2026-07-30 |
| [024](024-engine-in-a-worker.md) | Moving the engine into a Web Worker | **COMPLETE (2026-08-13, v8.32) — the worker is the default.** Open 2026-07-27 → 2026-08-13, eighteen releases dark. The gate it demanded was met: a measured frame timeline with blocking 129–137 ms → **0** and long tasks 1 → 0, throughput a wash. Latency was never the obstacle (0.100 ms round trip) — the cost was structural: atomicity, ordering, canvas identity, object lifetime. Five defects found only by driving the real thing (capture structs cannot cross `postMessage`; the surface transfers once per element; a mid-session flip unmounted the app), zero found by green gates. `ih_engine_worker=0` is the kill switch, next-load. Cloud path named open. Follow-ups: ADR-024-F1/F2 | 2026-07-27 |
| [025](025-focus-is-its-own-visual-channel.md) | Keyboard focus is its own visual channel, not the accent | **Draft** — records v7.58. Three affordances on three CSS channels were still one COLOR, so a stale focus ring and a live selection were indistinguishable (byte-identical CSS in the gallery). Focus is now `2px dashed var(--focus-ring)`, neutral ink. "Switch to `focus-visible:`" was already done and does not fix it — a mouse click measures `:focus-visible` false. Contrast went 2.67:1 → 14.3:1 | 2026-07-30 |
| [026](026-module-state-reaches-react-via-syncexternalstore.md) | Non-React module state reaches React via `useSyncExternalStore` | **Draft** — records v7.58's update dialog. Not a window `CustomEvent` (forbidden here) and not a Zustand store (wrong ownership; would drag PWA state into persisted-store territory). Snapshot must be a stable primitive. Includes the HMR trap: a dynamically imported module is a SECOND instance | 2026-07-30 |
| [027](027-original-collection-uses-a-derived-reachable-set.md) | Collecting originals uses a derived reachable set, not a stored refcount | **Draft** — records v7.59, the only known data-loss bug. Content-addressed originals are shared by design, so "is this still needed?" cannot be answered from one photo's own baseline. A stored count needs retrofitting onto four write sites and can drift; the derived set stores nothing and is the same mark phase the GC audit already computes. `db.ts`'s existing refcount counts an empty table — a guard-shaped hole | 2026-07-30 |
| [028](028-image-description-runs-locally-in-the-engine.md) | Image content description runs locally in the engine, not through a caption model | **Draft** — records v7.61 after the fact. `src/describe.rs` + `describe_image`; +5,364 B (+0.71%), no new dependency, works signed out. The rejected alternative is still half-built: `convex/ai.ts`'s `alt` job type has no model registered. ⚠️ Read the second consequence before "improving" the heuristics — the `portrait` misfire was re-measured 2026-08-01 and a threshold does not fix it; warm stone occupies skin's RGB region, so the next attempt needs spatial clustering | 2026-07-31 |
| [029](029-one-mutation-path-per-annotation.md) | A committed annotation has ONE mutation path, and a stale UI snapshot is fixed in the UI | **Draft** — records the fix for the seven-week shape-color bug. The briefed `set_shape_color(id, r, g, b)` was **not** added: `update_shape_annotation` already validated the id, snapped "Edit Shape" and wrote annotation data, and its own doc comment already said "or panel restyle". Both blockers were React-side. wasm unchanged at 761,213 B. ⚠️ `panelStylePatch` is a hand-maintained mirror of `DrawEditState["style"]` — a new style control missing from it fails silently on reselected shapes only, which is this bug one field wide **+ same-day follow-up:** the pre-mortem named the wrong failure. The diff compared the panel against ITSELF while reselect never synced it, so clicking the color the panel already showed was inert and never reached history. `selectShape` now seeds the baseline from the shape. | 2026-08-04 |
| [030](030-webgpu-runs-in-js-not-in-the-crate.md) | GPU acceleration runs in JS beside the engine, not as `wgpu` inside it | **Draft** — Phase 0 proven on Intel Xe-LPG: separable Gaussian blur in WGSL, **max channel delta 0** over 41,128 channels vs a faithful port of the Rust. `wgpu`-in-the-crate is ruled out by arithmetic, not taste: deploy-sentinel bounds the wasm at 700–800 KB and the crate is at 761,213 B. Opt-in via `ih_webgpu`, wasm unchanged. ⚠️ The harness cannot run in CI (no WebGPU on the runners), so `filters.rs`/`simd/blur.rs` and `blurReference.ts` are a matched pair with nothing enforcing it **MEASURED 2026-09-07** (Intel Xe-LPG, prod build): parity re-confirmed **delta 0**, then timed against the shipped SIMD `blur_region`. **There is no crossover — the GPU wins at N=1 at every size**: 1024² r5 is **93.3 ms CPU vs 8.0 ms GPU (11.7×)**, 2048² **512.4 → 29.1 ms**. Radius is the decider (CPU is O(r), GPU stays transfer-bound and flat): r30 @1024² is **645.3 ms vs 12.0 ms = 53.8×**. **Dispatch is only 6.5 ms of a 29.1 ms call** — 78% is the boundary, and an upload+readback control with nothing computed costs 23.2 ms. Batching 40 images takes 4× → **8.9×**, so batch is a direction, not a precondition. ⚠️ **`detect.ts` accepts `google/swiftshader` as `ok: true`** — headless Chromium in WSL2 hands out that CPU rasterizer under `--enable-unsafe-webgpu`, and benchmarking it would have read as "no speedup"; check the vendor string before believing a number. **ACTED ON 2026-09-09**: both free wins shipped (#92) — device+pipeline cached with `device.lost` handling (**warm 4.7 ms → destroyed 53.8 ms → warm 4.6 ms**, delta 0 throughout) and the redundant copy deleted; and the software-adapter refusal shipped (#94) — `probeWebGpu` now returns `ok: false` naming the adapter, matching `swiftshader`/`llvmpipe`/`lavapipe` as substrings (**WARP is a known gap** — too short to match safely). ⚠️ **The GPU can only serve the WHOLE-IMAGE blur**: the brush path is dabs below the transfer floor and `gaussianBlurGpu` has no region concept, and op-log replay is synchronous Rust that cannot await JS. Whole-image blur is **not** recorded as an op, so it carries **no replay-parity obligation** — the two paths do not overlap. ⚠️ wasm figures corrected: the band is **800,000–840,000 B** and the crate **816,971 B** (was 700–800 KB / 761,213 B — stale since ADR-045). ⚠️ `webgpu-types.d.ts`'s own tripwire FIRED (`GPUDevice.lost` added) — a second widening settles `@webgpu/types`. Harness: `scripts/webgpu-blur-bench.js`. Still one machine; CPU fallback ships forever either way | 2026-08-04 |
| [031](031-export-quality-lives-on-the-engine-snapshot.md) | Export quality lives on the engine Snapshot, and the persisted archive goes to v6 | **Accepted** 2026-08-05. A quality-only Apply pushes a `"Compress"` step that reverses nothing, because `snap()` captures pixels and quality changes none. The ONLY case of its kind: Phase 1 audited 108 exported mutators and found `Snapshot` already carries every other non-pixel parameter (opacity, visible, name, mask, annotations, dims, selection), all restored. ⚠️ ONE real format bump, not two: the binary cloud archive v5→v6. The local store is RAW IndexedDB with structured-clone values, so no `.version()` bump and no upgrade function — Dexie versions key paths, not value shape (precedent: `stale` on oplogManifests, `dexie/db.ts:93-96`). What remains is a read path tolerating `quality === undefined`. Read the pre-mortem: the rejected alternative (delete the marker) may be the honest one, and the warning sign is a SECOND non-engine parameter proposed for the Snapshot | 2026-08-05 |
| [032](032-ai-rename-uses-an-on-device-vision-model.md) | AI Rename names photos with an on-device vision model, cloud caption model as a paid upgrade | **Accepted** 2026-08-08. Supersedes part of ADR-028 — its "a caption model does not run offline" is out of date. **MobileCLIP-S0 vision tower, fp16, 21.8 MB**, lazy-loaded; the TEXT tower never ships (fixed vocabulary → embeddings baked at build time, ~131 KB, vs 146.6 MB for the naive both-towers download). `describe.rs` stays as the offline fallback; demo mode is why on-device is not optional. ⚠️ **q8 fails SILENTLY** — no error, no low confidence, just confident garbage (`mountain` 83% for a Tokyo street, `cat` 84% for a portrait); fp32 is identical to fp16 so 21.8 MB is the ceiling. ⚠️ **Confidence cannot gate quality**: widening 33→131 labels turned one photo from right-at-48% to wrong-at-73%. ImageNet classifiers excluded structurally (no "street"/"cafe"/"beach" class). Flag `ih_ai_rename_model`, default OFF pending browser numbers | 2026-08-08 |
| [033](033-the-text-box-has-a-height-and-the-op-log-goes-to-v4.md) | The text box has a height, and the op log goes to v4 | **Accepted** 2026-08-14 (v8.41). v8.40's "N/S would be handles that do nothing" was half right: height cannot drive REFLOW (line count is an output of wrapping) but it can drive LAYOUT. `box_height` is a MINIMUM with the text **top-aligned** — centering was built first and reversed before shipping, because it makes the top edge track the pointer at half speed and forces a second JS estimate of the text's natural height (~0.1·font_size of anchor drift). Corners move both axes; W/E stay width-only; no height-only handle, which is the cost of six. Format v3→v4 by v8.40's recipe: `#[serde(skip)]`, an APPENDED `Op::TextBoxHeight`, a fifth tuple element. No Dexie `.version()` bump (key paths unchanged — ADR-031's precedent). wasm +4,472 B (+0.57%). ⚠️ **The pre-mortem is the live one:** `decode_op`'s `ver != OP_FORMAT_VERSION && ver != 2` silently dropped v3 the moment 4 became current — every v3 op frame in every user's IndexedDB rejected, history quietly shortened. Caught by a test, fixed with a RANGE. ⚠️ Pre-existing, control-verified: a dragged box does not survive a reload on master either | 2026-08-14 |
| [034](034-perspective-is-projective-and-text-keeps-its-corners.md) | Perspective is a projective warp, and text keeps its corners instead of its pixels | **Accepted** 2026-08-17 (v8.43). Decision 1 briefly superseded by ADR-035 (v8.45) and **RESTORED by [ADR-036](036-perspectives-three-drag-rules-stay-three-sibling-tiles.md)** (v8.46) — three sibling tiles, as originally written. Decisions 2–4 never in question. Fills the `perspective` slot ADR-023 reserved. One quad, three drag rules, originally shipped as three Edit sub-tools — the single-tile version was built first and shipped BROKEN into the first browser run, because `SubtoolRow` renders the GROUP, so a mode with no sub-tool of its own has nowhere to appear. Rules stay in TS (a `postMessage` per `pointermove` at 120–420 Hz is the brush arc's banked-lag failure); only resampling is Rust, solved dst→src so there is no matrix inverse. Text corners stored **NORMALIZED on the annotation**, warp applied as the LAST tile stage — verified vector by forcing a rebuild 488×280 → 488×862 with the quad bit-identical. Format v4→v5 by ADR-033's recipe. ⚠️ **The skipped field's default is NOT its semantic default this time** — an all-zero quad is a collapsed point, not the identity, so `decode_op` normalizes or the recorder appends a `TextPerspective` on EVERY sync forever. ⚠️ Two bugs passed tsc + 591 tests + a hand-verified engine and were found only by driving it: a prop-identity re-render reset every drag to a rectangle, and re-applying against post-warp bounds COMPOUNDS the transform. Shapes named and deferred. wasm +16,796 B (+2.1%) | 2026-08-17 |
| [035](035-mode-placement-is-a-design-axis-siblings-get-tiles-facets-get-a-row.md) | Mode placement is a design axis — sibling modes get sidebar tiles, facets of one object get an in-panel row | **Superseded by [036](036-perspectives-three-drag-rules-stay-three-sibling-tiles.md)** same day (v8.46) — the placement was never the problem. Was: Accepted 2026-08-17 (v8.45). Supersedes ADR-034 decision 1. Modes that are separate things you pick up (Select's six kinds, Paint's four brushes) stay tiles in `SubtoolRow`; modes that are FACETS OF ONE OBJECT get one tile plus `ToolModeToggle`'s `showModeRow` — promoted from migration shim to supported layout. Perspective is the earning case: one quad, three rules about the other corners, so three tiles read as three tools. Its tile carries **no `mode`** deliberately — a mode-pinned tile goes dark the instant the row switches, which was the bug. Same change deletes the FOURTH copy of the sub-mode axis (`activateSubTool.ts` → one `selectModeOf()`), the copy that lit Distort whichever tile you clicked. ⚠️ Cost accepted, not hidden: two placements now exist, and the pre-mortem names the drift risk — a second panel enabling `showModeRow` without an explicit ask is the warning sign | 2026-08-17 |
| [036](036-perspectives-three-drag-rules-stay-three-sibling-tiles.md) | Perspective's three drag rules stay three sibling tiles — the highlight bug was the mode axis, not the placement | **Accepted** 2026-08-17 (v8.46). Supersedes ADR-035 entirely, restores ADR-034 decision 1. v8.43's "none of the three tiles highlight" was a stale FOURTH copy of the sub-mode axis in `activateSubTool.ts` (no `perspective` case → fallback lit `live[0]`, Distort, whichever you clicked). v8.44 read it as a placement problem and collapsed the three into one tile + in-panel row; **the real fix — deleting the duplicate axis — shipped in the same release and got the credit**. With each tile lighting correctly the collapse bought a second placement rule for nothing, so it is reversed and `showModeRow` goes back to being a shim nothing passes. ⚠️ Pre-mortem: nobody has yet watched the three-tile version WITH working highlights; the warning sign is Chris reaching for the wrong one of the three, which is a different complaint from v8.43's | 2026-08-17 |
| [037](037-the-wasm-sentinel-band-moves-and-bounds-a-toolchain-dependent-size.md) | The wasm sentinel band moves to 780,000–850,000 B, and what it bounds is a toolchain-dependent number | **Draft** 2026-08-18 (v8.49). The band had failed every release since v8.43's perspective (+16,788 B). ⚠️ **The two sizes in the record are the same build measured under different compilers**: local 806,967 B under rustc **1.92.0** (`ded5c06cf`) vs live **813,546 B** under **1.97.1** (`8bab26f4f`) — 6,579 B of rustc, not of code, confirmed by the `/rustc/<hash>/` paths inside both binaries. Nothing pins the toolchain (netlify.toml curls rustup `--default-toolchain stable`; CI uses dtolnay@stable), so **a size measured locally is not the shipped size**. ⚠️ **The floor was already vacuous** — 700,000 B while a featureless build had grown to 723,755 B and sailed through the check written to catch exactly that. Neither `ravif` nor `wgpu` fits under 850,000 either: raising does not create room, it stops pretending there was room. Pinning the toolchain is **filed, not dismissed**. ⚠️ Pre-mortem: a bound that only ever moves up is a logging statement with a failure exit code; the warning sign is a fourth band conversation | 2026-08-18 |
| [038](038-the-rust-toolchain-is-pinned-so-the-wasm-size-stops-moving-on-its-own.md) | The Rust toolchain is pinned, so the shipped wasm size stops moving on its own | **Draft** 2026-08-18 (v8.53). Closes the finding ADR-037 surfaced: nothing pinned Rust (`netlify.toml` curled `--default-toolchain stable` AND set `RUSTUP_TOOLCHAIN`; CI used `dtolnay/rust-toolchain@stable`), so the shipped binary was compiled by whichever `stable` existed on build day — **806,967 B under rustc 1.92.0 vs 813,546 B under 1.97.1**, and a band around a number that moves on its own is a band around nothing. Pinned to **1.97.1** with clippy, rustfmt and `wasm32-unknown-unknown` declared. ⚠️ **`RUSTUP_TOOLCHAIN` OVERRIDES the file** (verified: it reports *"overridden by environment variable"*), so netlify.toml's copy had to go or the pin was a no-op on the only builder that ships. Both CI steps become `rustup show`. ⚠️ After updating locally the gap fell 6,579 → **139 B**, of which **135 is the build machine's own directory** (`/opt/buildhome/` vs `/home/clj/`, 5 chars × 27 paths) — local can never byte-match production without `--remap-path-prefix`. ⚠️ Pre-mortem: the pin goes stale invisibly and dependabot does not watch it; the warning sign is a fix described as "just use stable" | 2026-08-18 |
| [039](039-a-transparent-artboard-is-left-out-of-a-format-that-has-no-alpha.md) | A transparent artboard is left out of a format that has no alpha | **Draft** 2026-08-18 (v8.53). Every photo imports onto an artboard (ADR-016) whose fill defaults to TRANSPARENT, and ADR-016 also made "Include canvas" the export default — so the padded backing shipped with every export. **JPEG has no alpha and the encoder does not ask: it writes those pixels OPAQUE BLACK** (measured — a transparent margin round-trips as rgba(0,0,5,255) for jpeg, rgba(0,0,0,0) for png/webp; avif has no encoder in Chrome and silently falls back to png). Every JPEG therefore carried a black frame the width of the canvas border, on a setting nobody chose. Narrowest possible exception: when the format has no alpha AND the fill is transparent, the backing is dropped — opaque fills, PNG/WebP/AVIF and "Photo only" all unchanged. One predicate `includeCanvasInExport` shared by the download, the batch ZIP and the dialog's size preview, so they cannot disagree. ⚠️ **The Settings copy said "Photo only … the default" for a year** — written before ADR-016 reversed it — so the UI and the code each looked right. Copy corrected; the DEFAULT was deliberately not flipped. ⚠️ Cost stated: a JPEG can now differ in DIMENSIONS from a PNG of the same document, silently. ⚠️ Interior transparency is NOT covered (an erased hole still goes black) — needs a SIMD matte flatten, filed not built. Pre-mortem: the warning sign is "my JPEG is smaller than my PNG" | 2026-08-18 |
| [040](040-the-backing-canvas-is-opt-in-on-export-again.md) | The backing canvas is opt-in on export again — `exportCanvasBackground` defaults to false | **Draft** 2026-08-18 (v8.54). **Supersedes ADR-016's export-default decision** (the two-layer artboard import itself stands, `canvasArtboard` unchanged, padding still 10px). ADR-016 made "Include canvas" the default on 2026-07-13 arguing what-you-see-is-what-you-export; three things are known now that were not then: **the Settings copy never followed** (panel AND header still said "Photo only … the default", so UI and code each looked right and nobody ever CHOSE include), it **produced a visible defect** (ADR-039 — transparent backing written as an opaque black frame on every JPEG, unreported for a year because PNG hides the same padding), and it **silently changes exported dimensions** (240×160 photo exports at 260×180, so the resize panel's numbers are not what you get). ⚠️ ADR-039 still applies — it now governs the opt-in path only. ⚠️ **Existing installs are NOT migrated**: `normalize()` only fills an absent key, so anyone with the pref stored keeps their behavior. ⚠️ Cost stated: a default reversed TWICE cannot be recalled from memory — read the value, and treat older notes saying "include is the default" as wrong. Pre-mortem: the warning sign is "my export lost my border", or a third reversal proposed without re-reading ADR-016 | 2026-08-18 |
| [041](041-color-overlay-is-a-field-on-the-layer-not-a-layer-in-the-stack.md) | Color Overlay is a field on the Layer, not a layer in the stack | **Draft** — the first non-destructive layer STYLE in the engine: `Layer.overlay: Option<ColorOverlay>`, applied in `render_layer` AFTER shapes/text and BEFORE the mask (Photoshop's order), clipped to the layer's alpha so it tints, never fills. A stack-layer alternative was rejected because it changes `layer_count` and the op log's view of the document (ADR-016). ⚠️ **The single-visible-layer fast path copies without calling `render_layer`** — it needed an explicit `overlay.is_none()` opt-out or the style was invisible on exactly the common single-layer document; the guard is convention, and the NEXT style must remember it too. History snaps ONCE on None→Some (swatch + slider are the same call, a drag fires per pointer move), so one undo removes the whole overlay. Session-lived like `mask` — persistence is ONE shared dexie-migration, filed in PARKING_LOT. One bake helper shared with `merge_down`, which flattens the lower layer by hand and would otherwise silently drop its style. wasm +3,149 B local | 2026-08-28 |
| [042](042-appshell-shrinks-by-two-structural-changes-not-by-more-handler-extractions.md) | AppShell shrinks by two structural changes, not by more handler extractions | **Draft** 2026-09-02. Written after #45's first extraction landed (`persistActiveCanvas`, AppShell **3,814 → 3,718**). The whole file was inventoried before picking a second: its logic holds **247 named blocks, only 34 of them ≥15 lines**, totalling 1,255 — so extracting every block worth extracting still leaves ~2,560. The best single candidate was **103 lines, 2.7% of the file**, and the **993-line JSX return (26%)** is untouched by moving handlers at all. So the campaign's ceiling is about a third and the arithmetic does not reach the goal. Extraction stops being the strategy; the remaining reduction is **two separate changes** — split the JSX into sub-components (moves rendering), and let consumers read stores directly instead of receiving threaded props (moves state ownership, and DELETES the ~213 small glue handlers rather than relocating them). ⚠️ **Rank candidates by what a block READS, never by its declared dependency array** — `confirmDeleteAll` declares 2 deps and touches 11 identifiers, scoring 34.0 by the array and 6.2 by reality; it would have been the wrong first cut by 5×. ⚠️ Cost stated: #45 stays open longer and #74 stays blocked longer, and both named changes alter behavior surface where extraction did not. Pre-mortem: the warning sign is the AppShell `max-lines` ratchet going a full month without being lowered, or being RAISED to admit a new prop | 2026-09-02 |
| [044](044-a-shape-reorder-is-deliberately-not-replayable.md) | A shape reorder is deliberately not replayable | **Draft** 2026-09-04. Unblocks #62 (shape z-order), blocked three sessions on one question: does a reorder need an op-log entry, an `OP_FORMAT_VERSION` 5→6 bump and a dexie migration (#68)? **The premise behind #68 does not hold.** Shape annotations are not written to the log at their call sites at all — neither `add_shape_annotation` nor `remove_shape_annotation` records an op, both only `snap()`. The log **RECONCILES**, diffing engine shapes against `log_doc.shapes` (`lib.rs:1511`) — keyed by `id`, **order-insensitive**, and `ShapeParams` has no z field, so **a pure reorder emits zero ops no matter what the call does**. Z-order needs no new state: it IS Vec order (`render_layer` iterates the Vec, `layer.rs:217`). Decision: `move_shape_annotation(id, to)` snaps history and records no op, following `remove_object` / `rotate_90_cw` / `resize_canvas`. **#68 closes with NO code.** ⚠️ Cost stated: a reorder is NOT replayable from the log. What bounds it — **`oplog_engine_in_sync` compares the COMPOSITE PIXEL HASH** (`lib.rs:1572`), so a reorder that changes what you see is detected and falls back to snapshot undo, and one that does not change the composite did not matter; but it pays for that by breaking the log for the session. Pre-mortem: the warning sign is **"my shapes came back in the wrong order after reloading"** — the hash guard protects undo WITHIN a session and says nothing about a document rebuilt from the log on a later visit | 2026-09-04 |
| [045](045-the-wasm-build-is-reproducible-so-the-sentinel-band-narrows.md) | The wasm build is reproducible, so the sentinel band narrows to 800,000–840,000 B | **Draft** 2026-09-05. Supersedes ADR-037's band; closes ADR-038's open measurement. With #70's `--remap-path-prefix` deployed, **local and production are BYTE-IDENTICAL** (816,971 B, sha `24c103c7…`, 0 differing bytes) and two local builds are bit-identical. The 139 B gap is fully explained: 135 B was registry paths (dead), and the **"4 unexplained bytes" were never a constant — they were wasm-opt version drift**. `wasm-pack` uses a `wasm-opt` from `PATH` if one exists and otherwise downloads its own; this laptop has binaryen **116**, wasm-pack 0.15.0 fetches **117**, and the two optimizers produced binaries differing in **456,523 byte positions** whose totals landed 3 B apart. ⚠️ **Pinning wasm-pack pins wasm-opt too — but only on a machine with no `wasm-opt` on `PATH`.** ⚠️ **The sentinel's EXPORT CHECK WAS VACUOUS**: all three symbols matched a real featureless build — `oplog_` and `remove_object` via DOC COMMENTS that merely name them, `rect_select` because it is not feature-gated at all — so the size floor was the only thing catching a featureless deploy. Now matched as declarations (`NAME(args) {`), verified 1/1/1 featured vs **0/0/1 featureless**. Band 70,000 → **40,000** wide; floor is **79,096 B above a measured featureless build** (720,904, not ADR-037's estimated ~730,000). ⚠️ Cost stated: a dev with binaryen on `PATH` silently builds a different binary and nothing warns. Successor recommended: assert the **sha256** instead of a band, now that the build is reproducible | 2026-09-05 |
| [046](046-the-deploy-sentinel-asserts-an-exact-hash-carried-by-the-bundle.md) | The deploy sentinel asserts an exact hash, and the bundle carries its own expectation | **Draft** 2026-09-05. ADR-045's named successor, built on the optimizer pin. The Netlify build writes `www-dist/build-info.json` (commit + sha256/bytes of the **published** asset + toolchain), and the sentinel asserts two tiers: **(1) self-consistency** — the served wasm is the one that build wrote (catches stale CDN / partial deploy), which cannot suffer deploy lag because file and asset come from one build; **(2) reproducibility** — CI's independent build of the **same commit** produced the same bytes, compared only when commits match, else reported as lag. ⚠️ **The expectation CANNOT be a stored constant**: a 7-line COMMENT in `ops.rs` changed the binary by **5 bytes** (#69 — panic `Location`s embed line numbers), so any committed digest is stale on the next docs PR. ⚠️ **A 200 IS NOT PROOF A FILE EXISTS** — Netlify's SPA fallback serves `index.html` for unknown paths, and an earlier version read that as "malformed" and **failed a healthy deploy**; the body must be JSON first. ⚠️ Toolchain provenance is recorded BY the build (`pkg/.build-wasm.env`), never re-derived — the wrapper's PATH swap is process-local and a later `command -v wasm-opt` wrote `binaryen: 116` for a 117 build. Size floor KEPT as an independent check (the export check was vacuous; the floor was the only real featureless detector). ⚠️ Currently **fail-OPEN on a missing `build-info.json`** so the first post-merge deploy is not blocked — flip to fail-closed once one is live; the pre-mortem's warning sign is that ABSENT line in a log for a recent deploy | 2026-09-05 |
| [047](047-layer-emptiness-is-a-question-you-ask-not-a-field-you-carry.md) | Layer emptiness is a question you ask, not a field you carry | **Draft** 2026-09-05. Unblocks #71 (three sessions blocked — on a DESIGN question, not a missing capability). New primitive `layer_is_empty(&self, index) -> bool`: pure `&self`, index bottom→top as `get_layers()` emits, out-of-range `true`, 8-byte word scan testing both alpha lanes with one mask, early exit. ⚠️ **NOT a field on `get_layers()`** — that runs in `capture_ui_state` at **199 call sites including per-stroke paths**, so #63's annotation counts could ride there (`Vec::len()` is O(1)) and a **4.0 ms** scan cannot. #63 and #71 look like one change and are not. ⚠️ **A different algorithm from `begin_layer_resize_preview`, not an extraction** — that one needs the bbox so it cannot early-exit, and it SETS `paste_preview`; answering a question must not move the document. ⚠️ **Mask deliberately ignored**: a hiding mask makes a layer invisible, not empty, and the overlay is clipped to the layer's own alpha BEFORE the mask (ADR-041). UI side is a **query, not derived state** — one layer, re-asked; a map-by-id is the exact shape of the **v7.81 batch-export data loss**. `undefined` disables nothing. wasm **+258 B**; async ratchet 127→128. Pre-mortem: the warning sign is `layer_is_empty` inside a `.map()` over `layers` | 2026-09-05 |
| [048](048-security-headers-ship-report-only-because-a-wrong-csp-only-fails-in-production.md) | Security headers ship, and the CSP ships REPORT-ONLY because a wrong one only fails in production | **Draft** 2026-09-07. Both sites sent HSTS and nothing else. Four headers (`nosniff`, `strict-origin-when-cross-origin`, `Permissions-Policy` denying camera/mic/geo, `frame-ancestors 'none'`) go **enforcing**; the CSP goes **report-only** because **nothing in this repo can catch a wrong one** — tsc, vitest, eslint, guardrails, the build and the deploy sentinel all pass on a policy that blanks the canvas, since none of them serves headers. ⚠️ Two directives would kill the editor: **`'wasm-unsafe-eval'`** (instantiating the engine counts as eval) and **`worker-src 'self'` — NOT `blob:`**, which was assumed necessary and is not: both workers are Vite's `new Worker(new URL(…, import.meta.url))` pattern emitting same-origin assets, **measured** (`engine.worker-BxZey_uo.js` / `codec.worker-BC0cZvq4.js` both 200 live). `blob:` IS needed in `img-src` — 8 `createObjectURL` sites. Origins inventoried from a **live session's resource timing**, not imports: self · `*.clerk.accounts.dev` · `*.convex.cloud`+wss · `*.ufs.sh` · fonts · ⚠️ **`cdn.jsdelivr.net`** (emoji-mart fetches its data set at runtime — found in the bundle, NOT reproduced live; drop the line if the reports never mention it). Headers live in `netlify.toml` and the **root** `vercel.json` only — not `marketing/vercel.json`, whose Root-Directory scoping is unreliable and would recreate the two-sources-for-one-build failure. ⚠️ **A report-only policy protects nobody** — the follow-up (drive a full session, fix the policy, then rename the header) IS the point; the pre-mortem's warning sign is this ADR still Draft with no follow-up PR | 2026-09-07 |
| [049](049-the-service-worker-is-blocked-on-eviction-reach-not-the-precache.md) | Activating the service worker is blocked on eviction reach, not the precache | **Draft** 2026-09-07. Successor to ADR-019 (which stands). Investigation only — **the SW was not registered**. ⚠️ **The feared sentinel collision does not hold.** The sentinel curls with no SW so it cannot see client-side staleness — but that is a DIFFERENT failure class, not a new blindness, and the plug already exists: `version.json` is `globIgnores`'d out of the precache, fetched `no-store`, and compared against `__IH_BUILD_HASH__` at boot **and at engine init**. `build-info.json` is outside `globPatterns` too, so tier 1 keeps reading the network. **Nothing to change in the sentinel.** ⚠️ **The real gap: `ci.yml` has NINE jobs and NONE run Playwright**, so the five `e2e/sw/` tests guarding registration, precache contents, offline reload and both skew cases are hand-run only — the `guardrails.sh` trap again, on the failure mode ADR-019 calls the worst class we ship. **Offline VERIFIED, not argued**: prod build with Clerk + Google Fonts blocked boots an identical shell (same 166 chars of UI text, canvas up, 9→8 buttons — the missing one is sign-in); editing, IndexedDB and the engine all work, Sample Images and sync do not. Demo-mode-is-sacred is what makes offline a complete editor rather than a degraded one. CSP is clear: `worker-src 'self'` covers `/sw.js`, and the sibling `workbox-<hash>.js` is same-origin — but Clerk's runtime **blob:** workers are the precedent, so report-only must still be live when the SW is first switched on. Engine is **817,392 B**, not ADR-019's 734 KB (+11%); precache ceiling 40 MiB, no cliff. Decision: **stay dark**, gated in order on (1) run `e2e/sw/` in CI, (2) rehearse eviction and **measure the returning-visitor tail** — the kill build only evicts someone who visits while it is the current deploy, and that number bounds the blast radius, (3) pick an update path (prompt-only / escalate after N / skipWaiting on a fresh tab — listed, deliberately not chosen). Pre-mortem: the gates are each someone's afternoon and none is urgent, so it sits dark another year; warning sign is a `vite-plugin-pwa` bump merging with no Playwright run in the PR checks | 2026-09-07 |
| [050](050-rotated-text-is-anchored-by-its-growing-tile-centre.md) | Rotated text drifts because it is anchored by its own tile's center | **Draft — anchor SHIPPED 2026-09-09 (#99), migration DECLINED on measurement.** Audit on the production origin (#100): **5 archives, 2 text annotations, 0 ROTATED**, and 0 annotations in op-log keyframes (Dexie store empty), so the archive count is the whole picture. Chris's rule was "migrate if anything but zero" — it is zero, so the anchor ships alone. ⚠️ A thin sample, stated as one: 2 annotations is not a survey. Anchor is **TOP-LEFT** (predictability — a box stays where you dragged it). ⚠️ **This ADR overpriced the migration**: `restore_text_annotation` does not take `tile_offset_x/y` and `editPersistence` strips `tile_*` on save because it is re-rendered on restore, so the delta is a pure function of stored fields — no per-annotation data, only a version marker the archive record lacks. ⚠️ **Red stamps are unaffected** — `commit_red_stamp` bakes pixels via `paste_region`, it is not a text annotation, so the -5° default in text.rs is a red herring. Pre-mortem's "anchor changed without the migration" is closed by the `rotated-anchor-pair` guardrail (scoped to the trig lines so prose cannot redden it, proven to fail). Originally 2026-09-08. Filed against "the text box moves/resizes as you type" — which is **two defects in one sentence**, and reproduced rather than guessed. **Unrotated text is CORRECT**: origin fixed at (820,404) across a 4x length change, only the width grows. **Rotated text TRANSLATES**, and in committed pixels not just the preview — driving the engine directly, ink minX/minY goes (107,104) → **(118,67)** at 30° for the same text lengthening. Mechanism: `text::rotate_pixels` rotates about `cx = w/2, cy = h/2`, so **the rotation anchor is a function of the text's own length** — add a character, the tile widens, its center moves, the result translates. ⚠️ **The DOM is not the bug**: `pivotLocalX = (measured[0]*scaleX)/2` reads the engine's own measurement so preview and commit agree (`transform-origin` drifts **10px → 130px** while typing, faithfully). Freezing the pivot in TS would reintroduce preview-vs-commit drift — a worse bug. **So this is an ENGINE change, not a leaf TS one.** **Deliberately NOT fixed**: the obvious anchor change makes **every saved rotated text annotation render in a new position**, with nothing to migrate it — three options costed (accept the shift / migrate `(x,y)` with a format bump + dexie-migration / document it), none picked. ⚠️ **NOT the same as ADR-024-F7** (dragged box lost on reload = the layer-JSON restore path hardcoding 0 for `wrap_width`/`box_height`) — checked; different file, different mechanism, **neither closes the other**. Pre-mortem warning sign: `rotate_pixels` changing without a matching `pivotLocalX` change — a matched pair nothing enforces | 2026-09-08 |
| [051](051-fonts-are-an-engine-feature-and-the-selector-today-is-cosmetic.md) | Fonts are an engine feature, and the selector shipped today is cosmetic | **Draft** 2026-09-08. Covers "other fonts aren't being added", the Fonts section, the premium picker and the font-format question — one feature, and the first finding decides all of it. **The ENGINE renders text**, not the browser: `src/text.rs` rasterises via `ab_glyph` with two `include_bytes!` faces (Liberation Regular **61,972 B** + Bold **61,520 B**), and `render_text(text, size, r,g,b, bold)` **takes no font** — a grep for `font_family`/`font_name` across `src/*.rs` returns **nothing**. So the 12-entry dropdown is cosmetic. ⚠️ **Three surfaces disagree**: box measured in Liberation (hardcoded), textarea glyphs in the SELECTED font, commit in Liberation — monospace diverges **+26.3%**, serif **−6.3%**. **A shipped vacuous control.** ⚠️ **THE BLOCKER: one more embedded face DOES NOT FIT.** Band 800–840k, live **817,392 B**, headroom **22,608 B**, one TTF **61,972 B** — short by ~3×. Embedding starts by moving the band, which gives up the only real featureless detector. Points at runtime-loaded bytes (`FontRef::try_from_slice` already takes a slice). ⚠️ **The privacy claim is ALREADY false** — Architecture.tsx says demo is "No account, no network" while `app/index.html:35-37` loads DM Sans + JetBrains Mono from Google on **every** load; confirmed on prod logged-out. Not caused by this feature. **CSP needs NO change** (verified on the live v8.70 response): `style-src` has fonts.googleapis, `font-src` has fonts.gstatic — but land the work while report-only is still on. **Premium gating can only be client-side** (AI uses `requireUser` + `TIER_DAILY_CAP`; a font picker has no server call) — recommend accepting it and **saying so**, not implying enforcement. **DECIDED 09-08 — the USER uploads the font.** Neither self-host nor fetch: a Pro user picks a `.ttf`/`.otf` off their own disk, stored locally, bytes handed to `ab_glyph`. Kills all three problems at once — never enters the wasm (band untouched), nothing fetched (no IP to Google), never redistributed (the user holds their own license). Runtime-loaded, only fonts installed in a past session. **UPLOAD IS FREE; SYNC IS PRO.** The gating question had no good answer while the Pro feature was the upload itself — it is entirely client-side, so no `requireUser` can stand in front of it and any label is defeated by a devtools flag. The upload consumes nothing of ours (their file, disk, CPU), which is why it cannot be gated **and why it does not need to be**. **Fonts-follow-you-across-devices** consumes storage, so it is genuinely enforceable server-side, same shape as `convex/aiJobs.ts` — and it is the better pitch, plus it retires the worst open item: **a Pro user opening their work elsewhere has the font there.** ⚠️ **Newly owed by this design**: what a document does when its font is missing on another machine (silent fallback looks like a bug); font bytes in IndexedDB are **user data → `dexie-migration` applies**; a malformed `.ttf` must not panic (`try_from_slice` returns a Result); a size/count cap (CJK faces run to MB); reject WOFF2 with a reason. Format: **TTF/OTF**, since WOFF2 implies moving rendering back to the browser. ⚠️ **The /architecture privacy claim is still wrong and is now a SEPARATE small job** — index.html's two Google fonts are unrelated to the text tool. Warning sign: a font PR that changes no file under `src/` is definitionally cosmetic **Partly resolved by ADR-058 (2026-09-11, Accepted)** — the three-surface disagreement is fixed and `register_font` ships, loading Liberation Sans/Serif/Mono at runtime with no font bytes in the wasm. The user-upload design here is **NOT superseded, it is unblocked**: `register_font` is its entry point and the shipped faces are its first caller. Still owed from this ADR — the upload UI, Dexie storage for font blobs, the Convex sync tier, and the `/architecture` privacy claim | 2026-09-08 |
| [052](052-oplog-records-six-of-sixty-seven-operations.md) | The op log records six of the engine's operation families, and the rest break it | **Draft** 2026-09-09. The survey behind backlog item 37, which was marked *ADR needed* with nothing behind it. The engine has **67 `snap()` sites** — 67 user-visible operations — and **6 recording paths**: `Stroke`, `Blur` (**the brush only**), `Crop`, `LayerMove`, `PerspectiveWarp`, and text/shape via the sync diff. ⚠️ **All six tonal adjustments are unrecorded** — `adjust_brightness/contrast/saturation/shadows/highlights/sharpen` each `snap()` then write straight into the layer buffer. So do whole-image blur, pixelate, redact, both flips, both rotations, resize, canvas size/border, compress, quality, emoji and red stamp. **Nothing corrupts**: ADR-013's composite-hash check catches the desync on the next undo and hands over to snapshot undo. But `oplog_broken` is reset ONLY on document load/restore (`lib.rs:992/1081/2088`), so **within a session it is sticky** — one Brightness nudge ends op-log undo for that document. ⚠️ **The fallback is not equivalent, and the byte cap binds before the count cap**: 50 snapshots / 512 MB means a 12 MP photo gets **~10 undo steps** and a 24 MP one **~5**, silently *(wrong by 2×, corrected 2026-09-18 below: ~5 and ~2)*. ⚠️ `Op::FillRegion` and `Op::Levels` have full `apply` + tests and **no producer** — pickaxe-verified NEVER wired (the same query returns `a46c919` for `Op::Crop`), and they are **not** dormant recorders for the adjustments: `Levels` is a black/white/gamma LUT that cannot express an additive brightness shift (needs a negative black, and `black` is `u8`) or saturation at all (cross-channel luminance lerp), and there is no `pub fn levels` for the UI to call. Decision: **record nothing new yet** — each op costs a Params type, an apply on the engine's own kernel, a format bump, a parity test and a recorder on the one-op-one-snapshot lockstep (#60/#61 broke it once). Cheapest real win is making the degradation **visible**, not adding ops. Pre-mortem warning sign: an undo-depth bug closed as "working as designed, see ADR-052" — this ADR used as a shield rather than a plan. **Amended 2026-09-18:** part 2 shipped 2026-09-11 (#127) as a once-per-photo toast; Chris retired it (popups "make our app look angry") for a quiet, always-visible status-bar readout, **"Undo NN%"** = estimated depth ÷ the History depth setting (100% while the op log drives undo, else `floor(history_max_bytes ÷ (W×H×4×layers))` clamped to 1..depth; never turns red). `history_max_bytes` finally has a caller. ⚠️ **The depth table was wrong by 2×**: a default document is Canvas + Photo and `Snapshot::bytes` counts both, so 12 MP gets **~5** steps and 24 MP **~2**; the old numbers stay in the ADR with the correction under them (browser check: 2068×1385 two-layer = "Undo 46%" = 23/50). The estimate counts exactly what `Snapshot::bytes` counts — every layer's buffer plus the selection mask while one is live (a first cut missed the mask; fixed before it shipped) | 2026-09-09 |
| [053](053-a-shapes-perspective-is-normalised-over-its-bbox-and-its-tile-is-padded.md) | A shape's perspective is normalized over its bbox, and the tile it warps through is padded | **Draft** 2026-09-11. Extends ADR-034 from text to every vector object the app draws — squares, circles, lines, arrows, pins, pen paths — after *"only works with raster"*. Three things are new: the basis is the BARE BBOX (the one rectangle TypeScript and the crate can both compute from the shape JSON, and a number they must agree on to the pixel because every reselect re-normalizes); the rendered tile is PADDED past it and the padding provably does not move the transform; and `NormQuad` makes the identity the type's default, because six shape constructors build with `..Default::default()` and an all-zero quad is a collapsed point. Op format 5 → 6 by the v3/v4/v5 prefix-extension recipe. ⚠️ Read the pre-mortem before adding a stroke term to either basis function | 2026-09-11 |
| [054](054-levels-is-one-curve-previewed-from-a-layer-copy-that-a-history-generation-guards.md) | Levels is one curve, previewed live from a layer copy that a history generation guards | **Draft** 2026-09-15 (#87). Gives ADR-052's orphan `Op::Levels` its first producer. `src/levels.rs`: black/white/gamma as **ONE curve for R, G, B** (Chris, 09-15; per-channel **deferred** — needs a new op variant and `OP_FORMAT_VERSION` **5 → 6**). Preview = one copy of the active layer at `levels_preview_begin`, recomputed through a 256-entry stack LUT per move (no compounding, no allocation); cancel restores; apply restores, snaps once, remaps, records one `Op::Levels`. A preview is never an undo step. `levels_lut` is the ONLY copy of the math — `ops.rs` `build_levels_lut` delegates, so live and replay cannot drift. New **`History::generation: u64`** (push, push_stroke, undo, redo, delete_entry, clear, both inject paths, clone-stamp `begin_stroke`) guards every preview write. ⚠️ **Undo depth is NOT the check**: undo then a new edit lands on the same depth with different pixels — the mutation turns exactly `a_stale_preview_is_dropped_never_written_back` red. UI: Levels tile shares the `effects` id, told apart by a non-persisted `effectsMode`; `setModeOf` silently refuses modes missing from `MODE_ACCESS`/`LEGACY_SUBMODES`. AppShell +1 prop line. wasm **823,714 → 829,481 B** (+5,767). ⚠️ **Recording Levels buys NO undo depth in the running app yet** — undo of ANY recorded op (a plain UI paint stroke too) falls back to snapshot undo and breaks the log, while engine-only runs and parity tests replay fine; the cause is in the app flush path, predates Levels, parked. ⚠️ Cost stated: an open panel holds a full RGBA layer copy (48 MB at 12 MP). Pre-mortem warning sign: anything claiming Levels preserves undo depth while that PARKING_LOT entry is still OPEN. ℹ️ **Superseded in NAME only by ADR-055**: `levels_preview_begin/active/cancel` are now `tonal_preview_*`, one slot shared with color presets — this ADR's text keeps the original names, as the record of the decision as it was made | 2026-09-15 |
| [055](055-a-colour-preset-is-a-named-stack-of-existing-filters-sharing-one-preview-slot.md) | A color preset is a named stack of filters the engine already has, previewed from the slot Levels now shares | **Draft** 2026-09-15 (#88). Builds directly on ADR-052 and ADR-054. **A preset is NOT a new primitive and NOT a new `Op`**: `src/presets.rs` `apply_stack` calls `filters::adjust_brightness → adjust_contrast → adjust_highlights → adjust_shadows → adjust_saturation`, skipping components at identity — no new pixel math, nothing for the op log to version. Sharpen is deliberately out (convolution + two scratch buffers, not color). ⚠️ **The ORDER is part of the definition** (tone before color), pinned by `a_preset_equals_its_filters_applied_in_the_documented_order` and `the_stacks_order_is_load_bearing`. ⚠️ **The five components are in DIFFERENT UNITS and passed through untouched** — brightness −1..1 fraction, contrast/saturation factors (1 = identity), **shadows/highlights ABSOLUTE 8-bit (−255..=255)**; `0.1` in shadows is a tenth of one level and silently does nothing. Normalizing inside the engine was rejected: it would make a preset new math and break the parity property. **Presets stay UNRECORDED**, exactly like the six tonal sliders (ADR-052) — deliberate, because undo of ANY *recorded* op currently breaks the op log in the running app (ADR-054, PARKING_LOT) while unrecorded ops fall back to snapshot undo and work. **ONE shared preview slot**: `LevelsPreview` → `TonalPreview` in new `src/tonal_preview.rs`; exports `levels_preview_begin/active/cancel` **RENAMED** `tonal_preview_*` (`levels_preview_set`/`levels_apply` stay Levels'). Two slots could both be open and the second to close would restore ITS copy over the other's committed pixels. ADR-054's `History::generation` guard moved with it and now protects both — mutating it reddens exactly `a_stale_preview_is_dropped_never_written_back` (levels) + `a_stale_preview_is_dropped_and_never_written_back` (presets), 15/17 vs 17/17 (re-run 09-15). Applying a preset is **ONE undo step**; the Quick Adjust grid it grew out of cost two. UI: third `effectsMode` on the `effects` id; the sidebar's `!== "levels"` became `=== "adjust"`. **Extended 2026-09-16 — Chris RETIRED the Quick Adjust grid**: `EffectsSettings.tsx` −62 lines (347 → 285), `PRESETS`/`applyPreset`/grid JSX/two imports gone, Adjustments is sliders only, presets live in one place. ⚠️ That also removed the disabled **4x Upscale** placeholder, which existed nowhere else — a user-visible "not yet" affordance gone (same call as #150), leaving two stale comments (`AISettings.tsx:5`, `toolConfig.ts:96`) and a marketing site that still sells "4× upscale" as Pro. Gates after the removal: tsc 0, lint 0 errors / 65 warnings (= master), vitest 820/820. wasm **829,481 → 832,962 B** (+3,481); SIMD opcodes **5563 → 5765**, twiggy attributing +2,572 B to `apply_stack` inlining the five SIMD filter bodies. ⚠️ **Measured, parked, NOT taken:** `#[inline(never)]` on the five `filters::adjust_*` delegators reclaims **2,351 B** and returns SIMD opcodes to exactly 5563 — held back because it edits five shared hot-path functions and the only bench (criterion, host) runs the scalar mirror, not the wasm SIMD path. Pre-mortem warning sign: a line in `apply_stack` that is not a `crate::filters::*` call, or a second `*_preview_begin` export | 2026-09-15 |
| [056](056-a-panels-bottom-actions-are-one-primitive-and-the-two-up-bar-pushes-them-apart.md) | A panel's bottom actions are one primitive, and the two-up bar pushes them apart | **Draft** 2026-09-16. "The action(s) at the bottom of a settings panel" was one job done **four ways across six panels** — `Button size="large" w-full`, two `Button flex-1` halves, a segmented Off/On `ToolButtonGroup`, a bare `ToolButton w-full`. New `PanelActionBar` + `PanelAction` (`app/src/components/ui/panel-action-bar.tsx`) owns the LAYOUT only: it **wraps `Button size="large"` and leaves `button.tsx` untouched**, because the button was never the drift — its own doc comment already named **"Apply Crop"** as the canonical `large` use and explicitly left width to the caller, which is the axis that fanned out. `layout="full"` is a one-column grid (a grid item stretches, so no `w-full` to un-set); `layout="split"` is **`justify-between`**, secondary left / primary right at their own label widths (Chris, 09-16, over equal `flex-1` halves). **Inline, not sticky** — last child of panel content, no new scroll container. **Icons dropped** from Crop, Color Picker and Remove Object (three of six carried one); `children` still takes a glyph. Rides with it: **`ToolButtonOption.active`** (`active={opt.active ?? value === opt.id}`, `aria-pressed` emitted ONLY for tiles that opt in — `aria-pressed="false"` on a non-toggle is a lie), which is what lets an independent toggle live in a single-select grid, serving both Guides' Lock and Rulers & Grid; and a new token **`--destructive-strong`** (#b91c1c light / #ef6a4a dark) for the destructive LABEL only, tint unchanged — "Remove canvas" measured **3.66:1 light / 3.98:1 dark** against WCAG 1.4.3's 4.5:1, now **≈4.9:1** both (the CSS comment and the re-measure differ in the second decimal on dark, 4.95 vs 4.91). ⚠️ **The split layout does NOT fit Canvas Size and that is OPEN, not decided** — at the real 226px column secondary **124** + gap **8** + primary **114** = **246px**, over by 20 before the dynamic `→ 1920×1080` suffix takes it to **325px**; it degrades by wrapping to a two-row staircase, **54px → 84px** idle. Better than the `flex-1` pair it replaces (109×**70**, labels over three lines) but ragged; three options costed (leave it / a stacked full-width `layout` / drop the redundant suffix), **Chris picks**. ⚠️ **Rulers & Grid changes BEHAVIOR**: 4 buttons → 2 toggles, and `onChange` fires unconditionally, so **clicking a lit toggle now turns it OFF** where the old "On" was idempotent. ⚠️ Tab order in Canvas Size flipped (Remove precedes Resize) — source order IS the layout in a `justify-between` row. Guides takes `columns={4}` (51×71, one row, 71px) over `columns={3}`, which matches the Wand grid's tile size exactly (70×71) but strands Lock with two dead cells. **Rejected:** canonizing the failing tint as a shared variant (it would make 3.66:1 the app-wide destructive standard). **Pre-existing, not caused here:** a lit `ToolButton`'s label measures **2.24:1** on the light theme — every lit tile in the app; `lib/styles.ts` logs the 2.67:1 NON-text version as "KNOWN, ACCEPTED" and the worse text shortfall appears nowhere. Pre-mortem warning sign: a `className` on a `PanelAction` that changes its WIDTH | 2026-09-16 |
| [057](057-threejs-is-a-marketing-only-dependency-behind-one-lazy-re-export.md) | three.js is a marketing-only dependency, bundled and reached through one lazy re-export module | **Draft** 2026-09-17 (`abadd514`). A new runtime dependency — `three` **0.165.0** + `@types/three`, both **pinned exact** — in `marketing/package.json` **only**: not the root, not `app/`, and the lockfile lists it under the `marketing:` importer alone. It backs **three** of the five figures in "The engine left the main thread" (FIG 1, 2, 4 are WebGL; FIG 3 is a bar table and FIG 5 a DOM timeline). Bundled rather than fetched from unpkg as the design export did, because `marketing/vercel.json` sets `script-src 'self' https://www.googletagmanager.com` — no CDN. `engine-in-a-worker.three.ts` statically re-exports the **nineteen** classes the scenes use and is the chunk boundary; `figures.tsx` loads it with a dynamic `import()` behind an `IntersectionObserver` at `rootMargin: 25%`. Measured: **473,970 B raw / 116,614 B gzipped**, tree-shaken from three's **1,284,652 B** ESM build. Blast radius verified in `dist/` — one file references the chunk (the lazy import), **no prerendered HTML does**, and home/blog index/features/pricing/architecture never request it. ⚠️ **No engine impact and no wasm impact** — marketing site only; it cannot move the sentinel band (ADR-037/038/045), and someone will assume it does. ⚠️ It is the **second-largest asset the site serves** (main bundle 221,674 B gz), not the largest, and the largest off every critical path. ⚠️ Two things the design could not carry verbatim: three 0.165 **cannot parse `oklch`**, so tokens go through a **1×1 canvas read-back** with a rejected-`fillStyle` sentinel; and at ≤40rem annotation labels are hidden with `display: none !important` (the scene writes `display` **inline** every frame), headings staying. ⚠️ **Nothing tests the scenes** — the exact pin is an admission, not a policy. Pre-mortem: the warning sign is a `three` bump PR open past one dependabot cycle, or `from "three"` appearing anywhere in `marketing/src` outside the re-export module | 2026-09-17 |
| [058](058-fonts-arrive-at-runtime-and-dead-hinting-pays-for-them.md) | Fonts arrive at runtime, and dead hinting pays for them | **Accepted** 2026-09-11 — built, gated and smoke-tested; **not yet released** (targets v8.76). Resolves ADR-051's open questions; **does NOT supersede** its user-upload design, it unblocks it. Chris picked **"ship TTFs, `register_font` at runtime"** over embedding a second family (ADR-037's band does not have the bytes). `render_text`/`measure`/`wrap` take a `font_id`; faces are static assets in `app/public/fonts/` handed to the wasm at runtime — **no font bytes entered the wasm**. Liberation Sans/Serif/Mono 2.1.5, **SIL OFL 1.1**, subset with `pyftsubset` to the same **430 codepoints** as the embedded face, hinting dropped (ab_glyph runs no hinting interpreter); 29,748–31,924 B each. The served Sans is **byte-identical in outline and advance to the embedded face** — 430/430, **0** advance mismatches, **0** differing outlines. **The three-surface fix ADR-051 demanded first**: all three derive from one `textFontId`, and `engineFonts.loadFaces()` fetches each file ONCE and gives the **same ArrayBuffer** to `register_font` and `new FontFace(...)` — not the same font, the same bytes, so they cannot drift. Measured on prod build: Sans preview 196 / ink **159 px**, Mono 231 / **187 px**. ⚠️ **A SECOND inert dropdown was found**: #113 cut the Text tool's twelve-entry list and **missed the identical copy in `BatchSettings.tsx`**, which offered Georgia/Impact/Comic Sans for another eleven releases while every batch render came out Liberation Sans — both panels now read one list (`useEngineFaces`). **Op-log v5 → v6**, the prefix-extension recipe a 4th time: `font_id` is `#[serde(skip)]` on `TextParams` (wire layout still byte-identical to v2), `Op::TextFont` APPENDED, a SEVENTH trailing element on `encode_annotations`; v5 decodes with every `font_id` empty = the embedded face = what it meant. Pinned by `v5_blobs_still_decode_under_v6`, `v5_op_bytes_still_decode_under_v6`, `text_params_wire_layout_is_unchanged_by_the_font_id_field`. ⚠️ Unlike the quad step the skipped-field default and the semantic default AGREE (`String::default()` is `""` = embedded), so **no promoter was needed — check that next time, do not assume it**. **Dexie: no `.version()` bump** — `font_id?` is an optional field in a JSON blob, not indexed; precedent documented verbatim on `stale?` in `db.ts`. ⚠️ **MONOTONICITY IS LOAD-BEARING**: `textMetricsCache` keys on `font_id` and its safety argument is purity, which holds only while an id always means the same outlines — enforced by `fonts::register` refusing to overwrite (new contract test) and by `ensureEngineFonts` being awaited before a face can be SELECTED (measuring an unregistered face answers in the fallback, and that wrong answer is cached against the real id for the life of the page). **Neither has a runtime detector.** **BAND REVIEW WITHDRAWN 2026-09-17 — the ceiling does NOT move**, which is what the ADR is now titled for (retitled 2026-09-18, number unchanged). The branch proposed 840,000 → 872,000 sized against a 823,503 B baseline; master left that baseline 56 commits behind, Chris moved the ceiling to **860,000** on 09-16 anyway, and a second raise in two releases makes the sentinel a formality. Then the bytes turned out to already be in the binary: **`ab_glyph` runs no TrueType interpreter**, so **64,312 B** of the two `include_bytes!` faces is hinting that is carried and never executed — over HALF of each file (Regular 32,542/61,972, Bold 31,770/61,520). Stripping it takes the pair 123,492 → 60,020 B. Three builds, one toolchain (rustc 1.97.1, wasm-pack 0.15.0, binaryen 117): master **858,087** (1,913 under the ceiling); this branch merged + **hinted 877,311** — **17,311 OVER, and 872,000 would not have covered it either**; merged + **unhinted 814,202**, i.e. **−63,109** vs its own hinted build and **−43,885 vs MASTER**, landing 45,798 under a ceiling nobody touched. ⚠️ **The swap is NOT safe on master alone** — 858,087 − 63,109 ≈ 794,978 is **under the 800,000 floor**, the featureless detector; it is safe only carried together with the fonts feature. Licensed by a pixel check through the shipped `text::render_text`: **572 cases, 40,913,342 pixels compared, 0 tile-size mismatches, 0 differing pixels**, with an anti-vacuity control proving the harness can tell faces apart (Serif renders 409×44 vs the embedded 438×44). Pinned by `fonts::the_embedded_faces_carry_no_hinting`, mutation-tested. ⚠️ **Op-log step rebased v5→v6 to V7→V8**: v6 and v7 shipped for shapes while this branch sat unmerged, so `Op::TextFont` MOVED to the end of the enum (the shape variants are on users' disks, `TextFont` never was) and the tuple gained a NINTH element; the branch's two version-pin tests collided by NAME with master's and were renamed and re-pointed to `v7_*_under_v8`. Also: a `HashMap` for the six-entry registry cost **5,181 B** of wasm (846,213 → 841,032, RandomState/SipHash/RawTable, none previously reachable) and was replaced with a linear `Vec` scan — ⚠️ a comment in `src/fonts.rs` said **6,764 B** for a session, a guess written before the measurement; the ADR cited the measurement and was right, the comment is fixed. Gates: fmt/clippy clean, **435 Rust** + **811 TS** passed, tsc clean, lint 0 errors / 65 warnings (baseline), guardrails green but `dead-exports: 1` which **master also fails**; ⚠️ **`guardrails.sh` is edited in this diff and both baselines go DOWN** — `librs-lines` **4,912 → 4,831** (bindings moved into `src/fonts.rs`'s own `#[wasm_bindgen] impl`, `commit_text`/`measure_text` into `src/text.rs`) and `rust-panics` **47 → 46** (two test-only `expect`s annotated `// allow: rust-panic`) — lowered in the same commit as the extraction, per the script's own header, **not raised**, prod build + inert-class-audit clean, browser smoke 0 console errors. Mutations: default-face `set_text_font` kills **4/6** Rust tests, ask-before-register `availableFaces` kills **2/11** TS tests; ⚠️ **3 preservation tests were VACUOUS on the first pass** (default face compared against itself) and were strengthened with a `fallback_ink` guard. Pre-mortem warning sign: any change to `fonts::register` that removes the already-registered short-circuit, or a new call site measuring text without awaiting registration | 2026-09-11 |
| [060](060-an-edit-op-merges-the-fields-it-cannot-carry-and-the-archive-stops-being-an-allowlist.md) | An edit op merges the fields it cannot carry, and the archive stops being an allowlist | **Draft** 2026-09-20. F1 from the 09-19 QC: a committed text lost its typeface and its dragged box on reload (Liberation Mono 137 px of ink -> 117 px, proportional; Serif 94 -> 103, serifs gone). **Two losses, both fixed here.** (a) `Op::TextEdit` REPLACED its annotation, and the four `#[serde(skip)]` axes (`wrap_width`, `box_height`, `perspective`, `font_id`) decode as defaults because the payload physically cannot carry them -- replaying the captured PRODUCTION log: cursor 3 = serif/299, cursor 4 (TextEdit) = `""`/0. `Op::ShapeEdit` had the identical latent defect. Apply now MERGES (`carry_skipped_from`). (b) `stripLiveAnnotations` was an ALLOWLIST and the engine's `wrap_width`/`box_height`/`perspective` were never on it -- emitted since v8.40/41/42, never once on disk. It is a denylist now, `parseSnapshotAnnotations` IS that function (it is the one feeding `restoreLayerStack`), and `restore_text_annotation` gained the three arguments its own comments called a gap 'the op-log path covers' -- FALSE: in every measured run where the box was dragged before the first save, NO op log was persisted and the resume landed on that path. &#9888; **NO VERSION NUMBER WAS TAKEN: `OP_FORMAT_VERSION` stays 8 and #187 keeps v9.** The wire layout is untouched; a bump would make every shipped build reject the new logs to fix a bug that needs no new bytes. The honest cost is that v8 bytes replay DIFFERENTLY than under v8.80 -- always recovering data the log already contained. Migration proven on bytes, not reasoning: the v7 fixture goes `wrap_width` 0 -> 338 (tile 359 px single-line -> 289 px wrapped) with no rewrite. &#9888; **`oplogManifests.formatVersion` is NOT `OP_FORMAT_VERSION`** -- it is the literal `1` at both write sites with restore rejecting anything else, so making it 'honest' would discard every log on every user's disk. Comment fixed, field untouched. `PersistedAnnotation` gains three optional unindexed fields: no Dexie `.version()` bump, same as `font_id`/`shadow_*`. Guard (c) rebased: `editPersistence.stripDrift.test.ts` built its input from `Required<PersistedAnnotation>` -- the app's own allowlist -- so it was blind by construction; it now derives the expected key set from the engine's captured JSON, and a Rust test (`the_captured_engine_json_still_carries_every_key_the_engine_emits`) goes red first if that fixture goes stale. 6 red tests green; wasm **+182 B** (814,202 -> 814,384). Pre-mortem warning sign: a new `#[serde(skip)]` field in a diff that does not touch `carry_skipped_from`, or an `Op::*Edit` apply arm that reads `*t = p.clone()` | 2026-09-20 |
| [061](061-sync-is-a-document-layer-and-the-archive-is-not-in-it.md) | Sync is a document layer, and the archive is not in it | **Draft** 2026-09-21. "All my devices should show the same thing when I'm logged in — between tabs and between devices." The unit is a named, canonically-serialized JSON blob replicated whole, last-write-wins: three of them (`prefs`, `ui`, `tools`). TWO HOPS, ONE ADOPT — BroadcastChannel across tabs (instant, no account, works signed out, which is the path that had no sync at all before) and one reactive Convex query over a generic `sync_docs` table across devices; both land through the same `adopt`, so there is one code path by which state changes from outside. The only decision — adopt / push / hold / idle — is a PURE function with no imports (`reconcile.ts`), enumerated in its own spec. Equality is a string compare, not a hash: a hash could only add a collision whose symptom is "my change didn't sync". A document is pushed only when locally DIRTY; the pending change lives in a per-account LEDGER (localStorage, every tab) that survives a reload and records the value owed, not a flag. ⚠️ THE READY GATE is the subtle part: rehydration from IndexedDB arrives as an ordinary zustand update, so a subscription attached before it would read the user's own persisted settings as a change they just made and push the laptop over the phone on every boot. Replaces the hand-rolled `users.settings` pull/push in `lib/preferences.ts` (seeded once from it, then legacy); leaves `user_colors` / `recent_texts` on their own tables — a row per item is right for a capped list, a blob is not. ⚠️ THE ARCHIVE IS EXPLICITLY OUT, blocked on the open op-log entry in `PARKING_LOT.md`. Fixes `rulerUnit`, which was never in `serializePreferences` and so had never survived a reload. **Amended before merge, after review:** revisions decide and the server compare-and-sets on the base revision; first contact adopts; one pushing tab per device (the tab claim) with backoff; Forget leaves `value: null` markers and clears `users.settings`; a format number on both hops; navigation from another device waits for the next load; the consent switch and any device id stay off the wire. **Amended 09-22-2026:** after v8.84 left a Win11 PC and an Android phone with zero pushes and zero `sync_docs` rows, each device gets its own sync switch (localStorage, never synced; off = signed out to the layer, on again = first contact, which is the reset) and a Send button, shown only on an empty account, as the one explicit exit from "never seed an account from an unchanged device". Reverses the old "no switch, sign out instead"; Sync becomes its own Settings tab and renders on the phone too | 2026-09-21 |
| [062](062-markup-written-twice-becomes-a-ui-primitive-and-fallows-exemptions-live-in-one-commented-config.md) | Markup written twice becomes a ui/ primitive, and fallow's exemptions live in one commented config | **Draft** 2026-09-22 (`refactor/ssot-ui-cleanup`). Rule: UI hand-written in 2+ places becomes a component (mostly `components/ui/`), a class string or color literal repeated 3+ times becomes a named constant (`lib/styles.ts` or module-local). New: `select-field`, `status-note`, `pane-heading`, `confirm-dialog`, `number-field`, `segmented-tabs`, `swatch`, `ParkedScreen`, `HintTooltip` + `IconButton tooltip`; `MASTER_BAR_CONTENT_BOX` ends the three-copy `top-[58px]`; seven shadcn tokens become `var()` aliases of brand tokens. `--primary`/`--accent` and `--ring`/`--border-active` deliberately NOT aliased (Chris, 09-22: keep separate for a future accent picker). fallow exemptions live in root `.fallowrc.jsonc`, one reason per entry — `scripts/*` are entry points because `guardrails.sh` runs `dead-exports-audit.mjs`. Pixel diff over 53 surfaces: six intended changes only. Bundle −26.7 kB; fallow dupes 2,938 → 2,824, unused files 13 → 0, unused exports 53 → 0. Costs: more files, the one-caller-prop trap, and PR #198 conflicts in `SecurityPane.tsx`. Pre-mortem warning sign: a `components/ui/` prop exactly one caller passes, or a `.fallowrc.jsonc` entry with no comment | 2026-09-22 |
| [063](063-a-share-link-stops-itself-by-derived-limits-keeps-each-view-as-a-bare-timestamp-and-a-scheduled-write-makes-its-end-date-real.md) | A share link stops itself by derived limits, keeps each view as a bare timestamp, and a scheduled write makes its end date real | **Draft** 2026-09-22 (`feat/shared-links-pane`). New Settings › Shared, the first caller `listMine` has had since the table was made. `shares` gains five OPTIONAL fields (`maxViews`, `expiresAt`, `pausedAt`, `lastViewedAt`, `expiredAt`). One pure `availability(share, now)` returns live, paused, expired or views, in that order, so raising a cap un-stops a link with no extra write. Reaching a limit PAUSES the link and Delete stays separate (Chris, 09-22-2026). Public `get` returns a union: a live link returns the pre-feature fields exactly plus `status`, so v8.86 keeps working; a stopped link returns NO `imageUrl`, withheld server-side. New `share_views { shareId, at }`, indexed `by_shareId [shareId, at]`, stores a TIMESTAMP ONLY (no IP, no browser, no account), and the privacy policy says so. ⚠️ A Convex query is NOT re-run as the clock moves, so a cached `get` would serve the image past its end date. `setLimits` schedules internal `shares.expire` at `expiresAt`, and the job's write is what stops the link; disabling the schedule turns 2 tests red. Deployed to `brave-ant-608` before merge: 77 functions, 4 new. Costs: `share_views` has no retention, `listMine` scales with 30 days of views, days are UTC, and a moved date leaves a job that does nothing when it runs. Pre-mortem warning sign: an `expiresAt` write outside `setLimits`, or `share_views` gaining a field besides `shareId` and `at` | 2026-09-22 |
| [064](064-a-feature-reaches-a-few-people-before-everyone-through-a-per-device-opt-in-ring.md) | A feature reaches a few people before everyone through a per-device opt-in ring, and nothing about that choice leaves the browser | **Draft** 2026-09-22 (`feat/beta-features-pane`). Chris, 09-22-2026: "how can you make it so you can test a roll out to a small amount of users". THREE RINGS: ring 1 = Chris (Super User + the nine `ih_*` switches, 7 kill / 2 opt-in); ring 2 = invited people, per device, from Settings — this ADR; ring 3 = a percentage of signed-in accounts from the server, deliberately NOT built (it turns a feature on for someone who never asked). Before this the only way in was typing a key into DevTools, so the ring was one person wide. `lib/beta.ts` is a VIEW of the existing `featureFlags.ts` registry — a flag joins the ring by growing a `beta` block there, and the pane reads `flag.isOn()` / writes `setFlagOverride`, naming no key of its own. ⚠️ The first cut carried its own `{id, key}` list — the second-source-of-truth failure that registry's header names — caught in review before merge. Building on the registry also caught its `ih_webgpu` row claiming "no pixel goes near the GPU yet", stale since whole-image blur grew its GPU path. ⚠️ **Nothing leaves the device**: no account, no id, no request — the per-browser device id `identity.ts` deleted is why a percentage bucket was not built, and no privacy-policy change was needed. `?beta=<id>` opts a device in then strips ONLY that parameter (`?v=` and the `#/` route survive, pinned by a test); `?beta=none` clears every one; applied once in `main.tsx` before React renders and before the WebGPU gate reads its key. Off is the default and the fallback — anything that is not exactly `"1"` is off. Settings › Beta commits as pressed, no Apply. 19 new tests, suite 1099; five mutations each killed. Costs: a switch is a promise, clearing site data silently opts someone out, the choice is per device, **no way to know who is in the ring** (no usage signal), an invite link is forwardable, "10% of users" still impossible, and `ToggleButtonGroup` has no `aria-pressed`. Pre-mortem warning sign: a `BETA_FEATURES` entry whose `key` no module reads, an `isBetaOn` call in a render body, or a second place that writes `ih_*` keys | 2026-09-22 |
| [065](065-the-ui-vocabulary-is-measured-before-it-is-decided.md) | The UI vocabulary is measured before it is decided, the instrument is not the gate, and every exception is registered | **Draft** 2026-09-23 (`docs/ui-constitution-night1`). Night 1 of 7, and it changed **no component, no class and no baseline** — it counted. The design vocabulary had never been measured, only asserted: **82 distinct spacing values across 713 uses** in 91 files, and **81 values used exactly once** across every family. `scripts/ui-inventory.mjs` (read-only) prints a histogram per family — heights, spacing, radii, icon sizes, type, z-layers, raw colors, arbitrary px — and the canonical set is picked from what the code does, so every step can name the count that earned it. ⚠️ **The measurement overruled the proposed scale `4 6 8 12 16 20 24 32` in two places**: `5` / 20px was proposed and is OUT at 19 uses, `0.5` / 2px was omitted and is IN at 24 — more used than the step that displaced it — and `2.5` / 10px (27 uses) is retired deliberately rather than unknowingly. Radius came out sharper than "pick a house style": against the shipped CSS, `.rounded` is a hardcoded `.25rem` Tailwind default and `.rounded-xl` reads Tailwind's own `--radius-xl`, so **78 of 221 radius uses bypass the house tokens** and `rounded` (60) duplicates `rounded-sm`'s 4px by a different route. ⚠️ **The instrument is NOT the gate.** The script reports, `guardrails.sh` ratchets, tonight it only reports — a grep reads text, and the survey proved it on itself three times: the histogram's `rounded-square` (`GalleryBar.tsx:266`) and `z-1` (`PenOverlay.tsx:612`) were words in comments; then `aria-pressed` was reported as **21** because `rg -c` counts lines containing the string and eight were comments (two inside `tool-button-group.tsx`, explaining when the attribute is emitted); counted as `aria-pressed={` it is 13, and one of THOSE is still prose (`MasterBar.tsx:93`), so the real figure is **12 in 12 files**. CI has historically gone red on `role="button"` in an explanation of the `role="button"` rule and on the English phrase "as any other dependency". A rule graduates at Night 7 only if a grep can check it without going red on a sentence: R1/R3/R7 qualify, R5/R6/R9 need a parser or a human. Anti-vacuity is built in: no working ripgrep = refuse, and every family carries a probe literal that prints `VACUOUS` and exits 1 rather than a reassuring zero. Third part: **every exception is registered**, `UI_EXCEPTIONS.md` seeded from the exclusions hiding inside `guardrails.sh`, same-line `// allow: …` preferred over a file exclusion (an exclusion covers code not yet written; an annotation dies with its line). ⚠️ Measured: of the six `raw-colors` file exclusions **three hide zero** (`CanvasArea.tsx`, `PenOverlay.tsx`, `lib/colors.ts`), all six files exist on disk, and the arithmetic closes exactly — 22 with exclusions + 7 GalleryBar + 2 CompareSlider + 1 MagnifierOverlay = **32 with none**; the `allow: raw-color` hatch is used **0 times in `app/src`**. Costs: the highest-value defect found is NOT fixed, and it is wider than the component that led to it — `ui/toggle-button-group` documents itself as independent toggles while **12 of its 14 call sites** are exclusive choices (`PARKING_LOT.md` already had "9 of 11 callers" as an API smell, measured before v8.85–v8.90 added three panes) and all 14 emit no `aria-pressed`; `tool-button-group` emits it only for tiles carrying their own `active` — deliberately and for sound reasons its doc comment states — so its SELECT mode announces nothing, and `tool-mode-toggle` passes `value=` and inherits the silence. Across all 43 segmented-control call sites, **exactly 2 emit `aria-pressed`**, 3 are ACTION rows where silence is correct, and **38 are silent and should not be**, including the v8.85 Sync switch and the v8.89 privacy switch. WCAG 2.1 AA, the survivor needs the fix too, and the two primitives that get it right (`radio-cards`, `segmented-tabs`) are the least used at 1 and 2 importers. Night 2. Other costs: three docs to keep current with nothing recomputing them; retiring 10px + 20px is 46 call sites; and a class-based count is blind to raw CSS (`styles.css:1598`'s bare `kbd` rule carries `border-radius: 3px`). Two free tightenings surfaced and were deliberately left (`rust-panics` 46 vs baseline 47, `librs-lines` 4763 vs 4808) — a docs branch does not quietly edit the blocking gate. Pre-mortem warning sign: a number in `UI_INVENTORY.md` that `ui-inventory.mjs` no longer prints, a new `-g '!**/…'` with no row in `UI_EXCEPTIONS.md`, or any baseline going up | 2026-09-23 |
| [066](066-a-tolerance-change-re-runs-the-last-click-in-place-and-the-engine-keeps-the-seed.md) | A Tolerance change re-runs the last click in place, and the engine keeps the seed | **Draft** 2026-09-24 (`feat/select-coverage-live-tolerance`). **Amends ADR-022** (panel rule only: a setting the mode does not use is shown disabled with a one-line reason, no longer hidden). The Select panel's Tolerance slider is live. The engine keeps a `SelectionRetune {kind, x, y, mode, label, snapped, generation}`, set by `click_select` (wand / edge-aware / color range) and cleared by every other producer in `apply_produced_selection`; new exports `selection_can_retune()` and `selection_retune(tolerance, edge_threshold)`. A re-run combines with the PRE-CLICK base (the undo-stack top when the click pushed a step, the live selection when it was a no-op), so there is one undo step per click however far the slider moves, and Add / Subtract / Intersect stay right while dragging back down. Validity is `History::generation`, not stack depth (undo-then-new-edit at the same depth is pinned by a test). JS scheduler `lib/liveRetune.ts`: 70 ms debounce, one run in flight, newest wins, because retune MUTATES and one call per tick would queue a flood fill per tick behind the worker's FIFO port. Live below 8 MP (flood kinds) / 3.2 MP (edge-aware), on a 300 ms pause above. Measured 09-24-2026, prod build, worker: 2068×1385 wand re-run 31–43 ms, worst frame gap 29 ms; 24 MP wand 237–347 ms, edge-aware 822–899 ms. Also: `selection_coverage()` for the "Selected 18.4% · 2.1 MP" readout, INTERSECT as combine mode 3 in a standing Combine group, `ih_selection_bool` now kills only the modifiers. Rejected: JS re-calling the wand per tick (a step per tick; a union never shrinks), undo-then-reselect (history churn), preview-then-commit (same engine problem). Costs: wasm 814,432 → 816,586 B (+2,154), `lib.rs` 4,767 of the 4,808 cap, the Sobel map is recomputed per edge-aware run, and Contiguous / Anti-alias / Sample Layer are not done. Pre-mortem warning sign: a new `self.selection =` site in a diff that does not touch `selection_retune` | 2026-09-24 |
| [067](067-threejs-reaches-the-home-page-through-lazy-chunks-and-three-webgpu-never-meets-three.md) | three.js 0.170 reaches the home page through lazy `*.three.ts` chunks, and `three/webgpu` never shares a scene with `three` | **Accepted** 2026-09-24 (v8.96). Amends ADR-057, whose "one re-export module" rule had already drifted when #219 added a second one. `three` + `@types/three` 0.165.0 -> **0.170.0**, still pinned exact; the `three/webgpu` entry first appears in 0.167.0, so 0.170 is a choice, not a floor. RULE NOW: every runtime `three` import lives in a `*.three.ts` module reached only by dynamic `import()` (`import type` exempt). Two new home chunks, both importing `three/webgpu` only: `gpu-letters.three.ts` (103 instanced lit cubes, replacing the hand WGSL + Canvas 2D) and `horse-trot.three.ts` (`horse.glb` 181,792 B, 15 morph targets, parsed by hand because GLTFLoader imports classic `three` and mixing the two breaks `instanceof`). Cubes load at `rootMargin 300px`; horse is Home-only, desktop-pointer `matchMedia` decided before any request, then `200px`. The label reports the backend three actually got (WebGPU / WebGL 2 / none). Measured gzip -9: `three.webgpu` **155,305 B** (largest JS asset on the site, lazy), `three.module` 118,540, the four `.three` chunks 341-2,210; 0 of 24 prerendered HTML files reference any. Costs: 155 KB lazily on Home, real-GPU WebGPU check OWED (headless drew blank, control too), two cores, no gate. Pre-mortem warning sign: `grep -lE 'three\.(webgpu\|module)-\|\.three-'` over `marketing/dist/**/*.html` prints anything (bare `grep three` is vacuous: prose says "three") | 2026-09-24 |
